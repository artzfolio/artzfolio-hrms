const CACHE = 'artzfolio-hrms-v741-2026-09-04'; /* v739 (2026-09-04, ROLETOKEN SECRET SPLIT — SECURITY FIX): built on v737,
  AUDITED x2, GO. ONLY change: _v91RoleTokenSecret() now uses a dedicated, never-client-shipped
  'roleTokenSecret' config value instead of reusing the public apiToken as the HMAC secret that signs
  roleTokens. apiToken is shipped in cleartext in the public, pre-login config.json (by design — it's
  the base API bearer every client needs), so reusing it as the roleToken signing secret meant anyone
  who fetched config.json could locally forge a roleToken with role:'owner' (bypassing every
  module-gate-only action — calcAllSalaries, saveSalaries, getTransactions, etc — confirmed none of
  these call _checkOwnerToken; module gate is their only defense) OR role:'ess' for ANY empId
  (impersonating any employee's self-service session — found by this build's own 2nd independent audit
  pass, broader than the original R3 finding). Found by an independent ULTRAREVIEW security pass (R3),
  2026-09-04. handleRevokeRoleSession's {all:true} "sign out everywhere" path now rotates BOTH apiToken
  and the new roleTokenSecret together, preserving its existing behavior and client UI byte-for-byte
  (the owner still sees and copies a new apiToken exactly as before) while actually invalidating every
  outstanding roleToken via the new dedicated secret.
  DEPLOY WILL INVALIDATE EVERY CURRENTLY-ACTIVE SESSION, ADMIN AND ESS ALIKE (ESS login also issues a
  roleToken, role:'ess', via this same function — the 1st audit pass's "no employee-facing flow touched"
  claim was WRONG, corrected by the 2nd pass): every logged-in owner/manager/supervisor and every
  employee mid-session on the ESS portal will need to re-authenticate once. Kiosk PIN/face clock-in
  itself is unaffected (open action, no roleToken involved). The two short-lived kiosk tickets
  (tnc/kscan, 10-min TTL) that also use _v91RoleTokenSecret() self-heal in normal operation — only a
  ticket issued in the exact instant before this deploy could fail verification after, a negligible
  edge case (the user simply retries the PIN/face scan). Also fixed by the 1st audit pass before this
  header was written: the new roleTokenSecret key was missing from GAS's _SETTINGS_REDACT, so it would
  have leaked via the open getSettings action — added; and the Config-read-failure fallback previously
  reverted to apiToken (silently reopening the hole) — now returns a fixed non-public literal, matching
  the established _v328KioskSalt fallback pattern. NOT YET LIVE-VERIFIED — this build has not been
  deployed. 2nd audit pass recommends one real login smoke-test (owner + 1 admin role + 1 ESS)
  immediately after deploy — not a blocker, just cheap and worthwhile given this is auth code. See
  decision_log/2026.09.04_HRMS_v739_roletoken-secret-fix_v1.md. Prior header preserved below.
  ── v737 (2026-09-04, REPAIR-DUPLICATES BUTTON + SUPABASE CHECK/SYNC UI):
  Built on v736 (AUDITED x2, GO). Two independent, deliberately separate pieces of work:

  (1) THE F&F REPAIR-DUPLICATES BUTTON, REBUILT. v736's own post-audit split pulled this button out because
  it writes FnF_Records.Status, which the salary register reads to decide exit-month exclusion -- money-
  gating, so Rule 14 held it for its own version. The GAS backend (_v192FixDupFnFCore, existing since v192,
  owner-token gated, dry-run by default, refuses any group containing an Approved/Paid record) is completely
  unchanged. What is new here is the HTML: the button, the module-map entry, and two corrections the v9.0
  master prompt's own audit named for this rebuild --
    a. the tooltip previously claimed "applying is a separate, explicitly-typed confirmation" when the real
       confirmation is a plain yes/no dialog. Corrected the copy to describe what actually happens, rather
       than making the confirmation itself typed (a typed confirmation would be a bigger UX change than this
       version's scope).
    b. the all-superseded empty state used to hide this button from exactly the state that needs it (every
       settlement voided/rejected, nothing "live" to show) -- fixed so the button still renders there.
  NOT auto-deployed: per Rule 14, a money-gating write needs a live browser test before shipping, which this
  session does not have a verified way to perform against production. Staged, AUDITED, awaiting that test.

  (2) SUPABASE CHECK/SYNC UI FOR ANY TABLE, not just the 3 hardcoded ones. Settings' existing "Supabase
  Speed -- Sync & Health Check" card only ever offered Employees/Leaves/Attendance buttons, even though the
  underlying sbVerifyTable/sbMirrorTable actions already accept any table name on the server's allow-list.
  This left the owner with no way to check/sync the 9 tables the v9.0 master prompt's button list names
  (TnC_Clauses, Salary_Payment_Status, Dashboard_Sessions, Orgs, SalaryLadder, Employee_Badges, Leave_Balance,
  PT_Slabs, Roster) short of a raw API call. Added a free-text table-name box wired to the SAME existing,
  already-audited v245SbCheck/v245SbSync functions -- zero new backend code, zero new auth surface (still
  gated by the same _checkOwnerToken this card's other buttons already required). Non-money, read/mirror only.
  Prior header preserved below.
  -- v736 (2026-09-03, SYNC INTEGRITY + F&F DISPLAY):
  Built on v734, NOT on v735 (v735 failed 5 of its own 6 audit rounds; its money-tier F&F work is
  deliberately deferred to its own version and a live browser test).
  NO salary, leave, attendance or F&F CALCULATION is touched anywhere in this build.

  ── F&F display integrity (HTML) ──────────────────────────────────────────────────────────────────────
  BUG 2 — voided settlements rendered as live ones, on FOUR surfaces, not the one that was reported:
    (1) the FnF Settlement Records list had no status filter at all -> now hides Voided/Rejected by default,
        with a "Show superseded (N)" toggle that dims them and stamps them SUPERSEDED. The positional
        _fnfRecordsCache is set to exactly the rendered array, so the row buttons cannot open a wrong record.
    (2) the empty state can no longer say "No F&F records yet" when records exist but are all superseded.
    (3) v227DownloadAllFnF — the bulk export — was generating a formal settlement STATEMENT PDF for voided
        records. This is the worst surface: it produces a document that leaves the system and reaches a
        person. Now excluded, and the counts say how many were excluded.
    (4) _v157DerivedNoticeStatus (and its GAS twin _v157FnFByEmp) could let a VOIDED settlement decide the
        notice badge. Both now skip Voided/Rejected.
    All four now share ONE definition of superseded, ['Voided','Rejected'] — the same one
    _v156ViewSettlement and GAS _v192FixDupFnFCore already used independently.
  BUG 3 — PULLED FROM THIS BUILD (post-audit). handleFixDuplicateFnF has been fully built, routed and
    owner-gated in GAS since v192, and had ZERO callers in this file, same as before. Its repair writes
    FnF_Records.Status, a column the salary register reads to decide exit-month exclusion -- a money-gating
    write, so Rule 14 holds it for its own version with a real Preview-then-Apply UI and a live browser
    test, not bundled into a display-integrity build. No behaviour change here versus pre-v736.
  BUG 4 — every ALREADY-EXITED employee rendered a blank identity ("—" / "Shift —") on the F&F screen,
    because renderFnF fetched the unfiltered roster and threw it away without setting State._allEmployees,
    while boot loads State.employees with Active + Notice Period only. Three lines, no extra request. Fixes
    the read-only identity CELL (_empById already prefers State._allEmployees). Does NOT extend to
    loadFnFEmployee or _v508EnsureExitTypeFor -- both still resolve strictly via State.employees, so an
    exited employee still cannot be loaded into the calculator this way, and the exit-type ownership stamp
    still cannot pre-fill from a lookup that finds nothing. (An earlier draft of this comment claimed those
    two were also silently repaired -- checked against source and that is false; left unfixed here on
    purpose, since both are money-adjacent per the v510 comment on #fnf-exit-type, and Rule 14 requires
    live verification or provable correctness-preservation before a money-tier change ships -- neither is
    available this session. Real gap; scope for a dedicated, live-verified F&F version, not this one.)
  Plus a one-character latent crash in loadFnFRecords: _v565RetryGas can return null, and the old guard
    "res && res.ok === false" let that fall through to res.records -> TypeError -> a silently blank section.

  ── Mirror-integrity guards (HTML) ────────────────────────────────────────────────────────────────────
  Six fast-read wrappers checked only Array.isArray(rows) -- an empty array IS a valid Array, so a zero-row
  mirror passed through as authoritative: _v544AssetsFast, _v573POSHICCFast, _v573OneOnOneNotesFast,
  _v574OnboardingChecklistFast (the four originally found), plus two more an audit found this build had
  missed -- _v579LeaveBalFast and _v577OTClaimsFast, the highest-stakes of the six, since Leave_Balance's
  own screen offers a "Seed Leave Balances" WRITE action right next to a confidently-rendered empty state.
  _v544AssetsFast's own guard comment (CORRECTED post-audit) cites the real affected screen -- the
  owner-only admin Asset Register, not the ESS "My Assets" modal, which reads a different, unguarded code
  path and remains a separate, still-open instance of the same bug. Live Postgres has all six tables
  (Assets, POSH_ICC, OneOnOneNotes, OnboardingChecklist, Leave_Balance, OT_Claims) at 0 rows right now. Ten
  sibling wrappers already carried this exact guard; these six were missed. Now zero rows falls back to the
  untouched GAS path.
  The two per-employee wrappers guard the WHOLE TABLE, before their own filter, so an employee legitimately
  having no notes/tasks still renders instantly without a needless round trip.
  POST-AUDIT FIX (v9.0 master prompt review): _v579LeaveBalFast's own BULK branch (the no-empId path used by
  loadAllLeaveBalances, the admin bulk screen) was still missing this exact guard -- only the per-employee
  branch above got it. With Leave_Balance at 0 rows live, the bulk screen was still rendering "No balance
  records found. Click 'Seed Leave Balances' to generate" as fact, right next to that WRITE button -- the
  precise scenario this fix exists to prevent. Now guarded identically to the per-employee branch.

  ── Stale-client detection (HTML) ─────────────────────────────────────────────────────────────────────
  window.HRMS_EXPECTED_LATEST was frozen at 'v362' for 372 versions and was broken in both directions: a
  silent no-op in the ribbon (a NUMERIC compare, 734 < 362, is always false) and a permanent FALSE "this
  device is STALE" in the version dialog (a STRING compare, v362 !== v734, is always true). Bumped, and now
  asserted by check_drift.py as the 5th lockstep point so it cannot drift a third time.
  NOTE TO FUTURE EDITORS, CORRECTED POST-AUDIT: this whole header sits INSIDE the swCode template literal --
  never use a backtick or a dollar-brace in it. What actually happens when you do depends on the shape:
    * An unescaped backtick with no LATER unescaped backtick anywhere before the literal's real close (the case a v736 draft
      of THIS comment introduced, and self-caught) leaves swCode unterminated -- a real SyntaxError, and
      node --check on the extracted script block DOES catch this shape.
    * A PAIR of unescaped backticks (open + close) does NOT error. It silently ends the string early; the
      text between them becomes plain JS wedged into the assignment. Concretely, a string literal
      immediately followed by OR-OR and a number and then a second quoted literal is syntactically valid --
      the number is used as a tagged-template TAG for that second literal, which is valid grammar even
      though a number is not callable; OR-OR short-circuits before that tag is ever invoked, so it never
      throws. node --check CANNOT catch this shape -- it is syntactically valid JavaScript.
  KNOWN PRE-EXISTING ISSUE, found by a v736 audit, NOT introduced by v736, NOT fixed by v736: exactly this
  second shape already exists inside a v690 changelog line that quotes a short OR-OR-50 code fragment as an
  inline example (itself wrapped in a backtick pair, per that era's writing convention), which silently
  truncates the LIVE, CURRENTLY-DEPLOYED swCode string to ~39KB of changelog text with no service-worker
  body at all (self.addEventListener never appears in the truncated result -- verified by extracting and
  evaluating the literal). Impact is bounded: registration prefers the static physical sw_vNNN.js file
  (unaffected -- it is generated by extracting this region as plain TEXT via build_scripts/regen_sw_v736.py,
  which does not care about backticks); this blob is used only as the on-the-fly fallback if that file 404s,
  and would then fail to register any handlers. Not fixed here: untangling every backtick in ~39KB of
  multi-version prepended changelog history under time pressure is a large, unbounded surgical risk on a
  region this file's own rules say never to touch casually -- scope it as its own dedicated, live-verified
  version rather than as a side effect of an F&F/sync-integrity build.

  ── GAS ───────────────────────────────────────────────────────────────────────────────────────────────
  Six silent-failure modes in the Supabase mirror engine (never stamp a signature on a no-op; the two
  unconditional stamps; the silent column auto-strip; the reconciler's missing resume cursor; two sheets
  that had never synced at all). See the GAS version header for the full account.

  Prior header preserved: v734 (2026-09-03, PRODUCTION OUTAGE hotfix — Tier A):
  every screen was taking 30-150s or timing out. Root cause was NOT a code regression (doPost is
  byte-identical v705->v730) and NOT Supabase (healthy, 0.1-1.0s throughout). It was: a ~5s fixed floor on
  every Apps Script round trip x 13-14 round trips per login (10-11 for Approvals) against a 4-slot browser
  cap, and — the tipping point — getCachedObjects(SH.EMPLOYEES) silently exceeding CacheService's hard
  100KB-per-value limit (~293KB live, bloated by faceDescriptor), so all 149 of its call sites were doing a
  FULL uncached sheet read on every request. Fixes: A1 chunked cache (GAS), A1b caller-freshness (GAS),
  A2 63 pure reads no longer write an audit row (GAS), A3 read-cache key no longer split by client-only
  flags, A4 the 30-second logDashboardTime heartbeat no longer wipes the whole read cache, A6 a request
  aborted by our own 150s ceiling is no longer retried on top of itself + retry jitter.
  Full evidence: decision_log/2026.09.03_HRMS_outage-phase0-rootcause_v1.md */
  /* Prior header preserved: v733 (2026-09-02, kiosk auth-gate fix): the final 2
  permission gaps closed. updateFaceDescriptor -- an in-handler role check, zero HTML change, both real
  callers already carry a valid roleToken (the v420 Quick Add + Photo Scan flow requires real admin/manager/
  supervisor permission before it even opens). logDashboardLoginSelfie -- a new short-lived
  kioskScanTicket, minted server-side at the moment of a verified kiosk PIN/face match, threaded through
  _v171LoginSelfie()'s one real call site. AUDITED x2.
  Full detail: decision_log/2026.09.02_HRMS_v733_kiosk-authgate-fix_v1.md */
  /* Prior header preserved: v732 (2026-09-02, ESS self-service auth-gate fix): 6 of the
  7 v730-deferred permission gaps closed (addTicket/submitReimbursement/submitKRASelfRating/
  essRestoreSession/submitRegularisation/addTransaction) by adding each to the role-gate map -- the real ESS
  self-service flow is unaffected (a valid essToken already short-circuits this gate), this only closes the
  omit-essToken raw-caller bypass. GAS-only change -- zero HTML content/feature change beyond the version
  bump. Full detail: decision_log/2026.09.02_HRMS_v732_ess-selfservice-authgate-fix_v1.md */
  /* Prior header preserved: v730 (2026-09-02, systemic security sweep): 15 more actions
  fixed with the identical missing-gate bug as v729's unblockPIN, found via a mechanical sweep of all 481
  dispatched actions, independently vetted (4 parallel research passes + 1 consolidated adversarial
  re-verification, 22/22 confirmed). A 16th (logDashboardLoginSelfie) was drafted then reverted before
  shipping -- the 2nd audit found a real ordinary-employee caller (kiosk "My Dashboard") the draft missed;
  see the GAS version header for the full reasoning. GAS-only change -- zero HTML content/feature change
  beyond the version bump. Full detail: decision_log/2026.09.02_HRMS_v730_systemic-authgate-sweep_v1.md */
  /* Prior header preserved: v729 (2026-09-02, security fix): unblockPIN was reachable
  by ANY apiToken holder with zero role check (missing from both the role-gate map and the deliberate-open
  list) -- it clears an employee's PIN lockout AND applies a real PIN-reactivation-fee Transaction for an
  arbitrary empId, no approver check. Gated to 'employees', same posture as sibling resetPIN, call-site-traced
  admin-only (no kiosk/ESS caller). updateFaceDescriptor has the identical gap but is deliberately NOT fixed
  here -- its client caller is also reachable from an unauthenticated kiosk device (restricted photo/face-scan
  feature), so the same gate would break that real feature; needs its own scoped fix, flagged to the owner as
  a dedicated follow-up. AUDITED x2. GAS-only change -- zero HTML content/feature change.
  Full detail: decision_log/2026.09.02_HRMS_v729_authgate-facedescriptor-unblockpin-fix_v1.md */
  /* Prior header preserved: v728 (2026-09-02, Supabase-migration progress, owner-approved):
  promoted ExtraPay_Rules (4 rows, real 'RuleId' key) and Shift_History (84 rows, row-index-synthesized key)
  out of the once-daily/~5-min blob mirror tiers into real structured Postgres tables, same methodology as
  v726/v727. MONEY-TIER (unlike Leave_Types/SalaryBands): both are genuine calcMonthlySalary inputs
  (extra-pay-method resolution engine / _shiftPeriodsForEmp) -- GAS keeps reading EXCLUSIVELY from the Sheet
  in every payroll code path, confirmed unchanged; this promotion only gives Postgres a fresh mirror.
  Backfilled directly in the promotion migration (verified 4=4=4, 84=84=84), safety net + unconditional
  instant-mirror-on-save wired into all 7 real write paths from day one. ONE real HTML functional change this
  version (this file is NOT GAS-only, despite the first build's own now-corrected claim): the
  _v559ExtraPayFast Settings fast-read wrapper was still unconditionally JSON.parsing the old
  {_rk,rowjson} blob shape after promotion (silent, permanent, correct-but-slow fallback -- no data-loss risk,
  just zero speed benefit) -- fixed to use the shared _v596ParseMirrorRow helper (tolerant of either shape),
  same fix already proven for Activities at v634. Full detail:
  decision_log/2026.09.02_HRMS_v728_extrapayrules-shifthistory-structured-promotion_v1.md. */ /* v727 (2026-09-02, Supabase-migration progress, owner-approved,
  AUDITED x2, GAS-only -- no HTML functional change beyond this version marker): promoted SalaryBands (10 rows,
  per-level pay bands) out of the once-daily blob-backup mirror into a real structured Postgres table, same
  methodology as v726's Leave_Types -- backfilled directly in the promotion migration (verified 10=10=10),
  safety net + unconditional instant-mirror-on-save wired in from day one. Confirmed NEVER referenced inside
  calcMonthlySalary -- display/mirror-only, zero payroll calculation input change. Full detail:
  decision_log/2026.09.02_HRMS_v727_salarybands-structured-promotion_v1.md. */ /* v726 (2026-09-02, Supabase-migration progress, AUDITED x2, GAS-only --
  no HTML functional change beyond this version marker): promoted Leave_Types out of the once-daily blob-backup
  mirror into a real structured Postgres table (14 rows) on the ~5-min periodic rotation, with an unconditional
  instant-mirror-on-save AND the daily reconcile-watchdog safety net wired in from day one -- unlike 4 prior
  promotions (CompOff/TnC_Clauses/Notifications/KRA_Assessments) that shipped without one. Backfilled DIRECTLY
  from the existing blob mirror in the promotion migration (verified 14=14=14), not left for a future periodic
  tick -- that pattern is confirmed broken this session (TnC_Clauses, promoted at v670, is still 0 rows in
  Postgres a week later; root-caused, the one remaining fix needs the owner's PIN, flagged not attempted). Also
  resolved the "7 ambiguous 0-row dark reads" from the checklist: 6/7 confirmed genuinely empty and safe,
  TnC_Clauses is the one real gap. No payroll/salary/leave-balance logic touched. Full detail:
  decision_log/2026.09.02_HRMS_v726_leavetypes-structured-promotion_v1.md. */ /* v725 (2026-09-01, owner-directed display fix, AUDITED x2): the Attendance
  Records screen (both the single-employee day grid and the multi-employee table) was hiding a real OT session
  entirely on any day whose PRIMARY punch is a full-leave status (IL/UL/LWP/etc) -- Bishal Kumar's 7h OT on 29-Aug
  (an Informed Leave day) showed Clock In/Out/Hours/Extra-OT all as "--", even though otMinutes=420 was already
  computed correctly server-side (v158 WS-3.3's fold). Root cause: v116 DEF-A4's blanket "leave days show no time"
  display rule (_showTime = rec && !_isLeaveRow) also zeroed the folded OT figure and never carried the OT session's
  OWN clockIn/clockOut/workMinutes forward -- those were discarded during collapse/dedup, only the summed otMinutes
  survived. Fix (display-only, NO payroll/salary/leave-balance logic touched): GAS handleGetAttendanceMonthBulk now
  also captures the OT session's own clockIn/clockOut/workMinutes as new otClockIn/otClockOut/otWorkMinutes fields
  on the collapsed record; renderAttendanceGrid's own client-side fold does the same for the raw per-day rows it
  already receives. Client-side, both renderAttendanceGrid and renderAttendanceMultiTable: Extra/OT (badge + column)
  now shows whenever otMinutes>0, independent of leave status; on a full-leave day with a real OT session, Clock
  In/Out/Hours now display THAT session's own punch (never the leave's, which genuinely has none). Status badge,
  Late and Short are UNCHANGED (still blank on a leave day -- there is no lateness/shortfall against a regular
  shift to report). A normal Present+OT day is pixel-identical to before (all new logic is gated on _isLeaveRow).
  Full detail: decision_log/2026.09.01_HRMS_v725_leaveday-ot-display-fix_v1.md. */ /* v724 (2026-09-01, owner-approved security fix, AUDITED x2): handleRevertEntity
  in the GAS backend had ZERO authorization check despite being registered in the main dispatch table and reachable by
  anyone holding the shared API token -- it could void ANY PettyCash/Transaction/Bonus/OT-claim entry (via
  _v68VoidByStatus), OT claims (via _v68RevertOT), or Points entries (via _v68RevertPoints), regardless of current
  status, with no PIN/role check. GAS fix: added the identical _checkOwnerToken(p) gate already used by sibling
  destructive handlers (handleApproveNotice/handleRejectNotice/handleCancelNotice/handleDirectExit), placed as the
  first line before the lock is even acquired. HTML fix: revertEntityPrompt's Revert button (v68 Stage-2C, written
  before the v87 A2 owner-gating sweep) never called _ensureOwnerToken() unlike its money-adjacent neighbours
  (applyRecoveriesForMonth/applyRecoveriesFromRegister/approveOTClaim, all a few lines away) -- without this addition
  the owner would have hit the generic "access needs refreshing" error with nothing actually prompting for the
  password, in a dead loop. Added an _ensureOwnerToken() await-guard as the first line of the Revert button's
  callback, same idiom as those neighbours. Not ESS-reachable: revertEntityPrompt is wired only from the
  Transactions/PettyCash/OT-Claims/Points admin-tab-content screens, all behind switchTab's checkTabPermission
  gate -- no ESS employee screen calls the revertEntity action. Full detail:
  decision_log/2026.09.01_HRMS_v724_revertentity-auth-gate-fix_v1.md. */ /* v723 (2026-09-01): GAS-only, no HTML functional change beyond
  this version marker -- money-tier fix, owner-directed. PettyCash approve/reject/delete had no precondition
  guard: an already-Approved or already-Rejected entry could be silently re-decided (re-approved, re-rejected,
  or deleted outright) with no trace of the prior decision, same bug class as v722's handleRejectLeaveEncashment
  fix. handleApprovePettyCash/handleRejectPettyCash/handleDeletePettyCash now all require status==='Pending',
  matching the guard handleUpdatePettyCash already had. A second, separately-flagged bug (submitExtraDayClaim
  allegedly ungated) was investigated and did NOT check out -- it is already gated via
  _V91_ACTION_MODULE['submitExtraDayClaim']='otclaims' (same as sibling submitOTClaim) and is not even
  ESS-reachable; no code change made for that claim. Full detail:
  decision_log/2026.09.01_HRMS_v723_pettycash-reapproval-guard-fix_v1.md. */ /* v721 (2026-09-01): GAS-only, no HTML functional change beyond this version marker -- owner-directed money-tier bug fixes for the two live GAS issues disclosed during v720's build (decision_log/2026.08.31_HRMS_v720_8action-batch-and-ledger-corrections_v1.md), both AUDITED x2 before shipping. FIX 1: handleLeaveEncashmentApproval had no guard against approving the same request twice -- a double-click, stale tab, or retried request would debit Leave_Balance a second time AND create a second Bonus_Ledger credit row, paying the employee twice. Fixed with the minimal status==='Pending' guard already established elsewhere in this file for the identical bug class (handleApproveCompOff's own precedent) -- a request not currently Pending is refused outright before any write, with a clear message naming its real status. FIX 2: handleEncashCompOff's payroll-credit Transaction write went through handleAddTransaction (a thin forward to the ESS self-service shim), which force-downgrades every LeaveEncashment-type row to status Pending/approvedBy pending-approval regardless of what was passed -- silently turning every comp-off encashment's intended Applied credit into a stuck row needing a separate manual approval nothing in the flow ever surfaces, and skipping the Applied-only credit notification too. Fixed by calling the shared _v92AddTransactionCore directly (bypassing both the ESS status-forcing and handleAddTransactionAdmin's owner-token gate, which this module-gated-only action was never meant to require) under its own lock, mirroring handleAddTransactionAdmin's own lock-then-instant-mirror shape. Full detail: decision_log/2026.09.01_HRMS_v721_leaveencashment-compoff-bugfixes_v1.md. Prior: v720 (2026-08-31): Sheets-Independence campaign -- Supabase/gateway-only, NO HTML/GAS functional change beyond this version marker. 8 new dark write-RPCs added to the hrms-gateway Edge Function this round (gw45-2026-08-31), built by 5 parallel agents and merged after 2 rounds of genuinely independent adversarial audit (structural + non-money-tier re-verification, and a separate money-tier re-verification) -- both audits came back clean, no defects found: addStandingLateGrace/cancelStandingLateGrace (new Standing_Late_Grace table, in-handler role gate only, matching GAS exactly), consumeCompOff/encashCompOff (CompOff, module 'compoff' gate, per-employee advisory-locked atomic transactions), savePTSlabs (PT_Slabs, dual owner-token+module gate, full-table replace matching GAS's own wipe-on-empty-array quirk, live self-tested), saveExitFormClauses (ExitForm_Clauses, owner-token-only), upsertMaternityRecord (MaternityRegister, dual module+owner-token gate), approveLeaveEncashment (Leave_Encashment + cascading Leave_Balance debit + Bonus_Ledger credit, module gate only). All 8 ship fully dark on both layers -- none flipped in API_ROUTES, none of the touched tables added to EDGE_OWNED_TABLES; confirmed structurally that the gateway dispatcher refuses every one of them (not_flipped) before any gate or handler code runs, so zero live-user risk regardless of any undiscovered defect. TWO disclosed pre-existing LIVE GAS bugs surfaced during this build, neither introduced nor fixed here, both flagged to the owner separately: (1) handleLeaveEncashmentApproval has no re-approval guard -- a double-click in production today can double-debit an employee leave balance and double-pay them via a second Bonus_Ledger credit; (2) handleEncashCompOff force-downgrades its Transaction write from Applied to Pending via an ESS-only shim, so every admin-initiated comp-off encashment in live GAS today requires a separate manual approval and never auto-credits payroll. Also ledger-bookkeeping corrections this round: 9 campaign-log rows that were marked PENDING despite already being built/staged in prior sessions (handleDeleteOrg, handleSubmitReimbursement, handleApproveBonusClaim, handleRejectLateClockIn, handleRejectRegularisation, handleUpdateActivity, handleGetLoyaltyBonusAllBalances, handleSubmitKRASelfRating, handleSubmitKRAMgrRating) corrected to reflect their real staged status. Full detail: decision_log/2026.08.31_HRMS_v720_8action-batch-and-ledger-corrections_v1.md. Prior: v719 (2026-08-31): Phase 1 duplicate-attendance investigation (decision_log/2026.08.31_HRMS_phase0-verification-and-phase1-ot-drop-rootcause_v1.md) -- TWO GAS-only fixes, no HTML functional change beyond this version marker. FIX 1 [MONEY-TIER, owner-directed 2026-08-31]: calcMonthlySalary previously only credited a date's OT-session minutes into payroll when that date's PRIMARY attendance row had status P (Present) -- a day whose primary row is a leave/absence code (IL/UL/etc.) silently never paid its real OT-session hours at all, confirmed live on Khushi Kumari (AZ_EMP_0073) and Bishal Kumar (AZ_EMP_0105), 2026-08-29, and independently re-verified by a second, genuinely separate audit pass before being reported. Owner's explicit policy: the leave day itself stays exactly as-is (unpaid, unchanged); OT worked after official hours must be paid exactly like any other employee's OT, regardless of the day's leave/absence status. Fixed with an additive post-loop sweep (own OT_INSTANT_PAID guard, same grace-threshold/OT-2 rules as the existing path) that only fires for dates NOT already handled by the P-day path -- zero change for every normal month. Affects 30 employee-days across 9 dates back to June 2026, not yet applied to any past-month recompute (awaiting the owner's go-ahead on which affected months to recompute). FIX 2: the same investigation found the bulk/backfill attendance importer's stored clockOut date never rolled forward for an overnight session (a 19:00-to-02:00 OT session correctly computed 7h worked but stored clockOut as same calendar day as clockIn, i.e. apparently before clockIn) -- fixed by reusing the duration calc's own already-correctly-rolled Date object for the stored value instead of re-normalising the raw input a second time. Both fixes are additive/corrective only; every existing day-status count, unpaid-day deduction, and leave-balance calculation is untouched. */ /* v718 (2026-08-30): Sheets-Independence campaign -- approveBonus write-RPC (Bonus_Ledger), verified directly against live Supabase schema (all columns space-named, no PRIMARY KEY on "Bonus ID" despite being a real natural key in practice). Ports handleApproveBonusClaim exactly -- registered under GAS's real dispatch key 'approveBonus' (confirmed directly against source, NOT 'approveBonusClaim', the handler's own function name). Module-gated 'bonusmgmt'. Status-decision branch (Rejected/Claimed Instant/Approved) replicated exactly. DISCLOSED: GAS's real handler has no not-found branch -- an unmatched bonusId still returns {ok:true}, a likely latent GAS-side bug reproduced exactly per Rule 1, not silently fixed. Client API_ROUTES capability entry added. Full detail: decision_log/2026.08.30_HRMS_v718_bonus-claim-approve-writerpc_v1.md. */ /* v717 (2026-08-30): Sheets-Independence campaign -- deleteOrg write-RPC (Orgs), verified directly against LIVE Supabase schema before building (PRIMARY KEY(id), no unique constraint on type+name; the companion gw_org_add function's real definition was pulled live via pg_get_functiondef and confirmed to already exist, resolving a documentation-gap finding from v715). Ports handleDeleteOrg exactly: matches by (type,name) not id, module-gated 'settings' -- a real, disclosed GAS-side asymmetry with its ungated sibling addOrg, not homogenized here. Deletes exactly one matching row; cannot reproduce GAS's exact bottom-of-sheet tie-break for the rare duplicate-(type,name) case (disclosed in the migration comment), identical to GAS otherwise. Also carries forward v716's own audit-fix (raw-vs-trimmed empCode self-scope check) into this cp-forward chain. Client API_ROUTES capability entry added. Full detail: decision_log/2026.08.30_HRMS_v717_org-delete-writerpc_v1.md. */ /* v716 (2026-08-30): Sheets-Independence campaign -- submitReimbursement write-RPC (Reimbursements), verified against LIVE Supabase schema/constraints directly (not assumed) before building: reqId PRIMARY KEY, no unique constraint issues, table already actively instant-mirrored from GAS since v668 (_v668InstantMirrorReimbursements, on every real submit). Ports handleSubmitReimbursement exactly: same reqId generation shape ('R_'+8 hex chars), same required-field guard (empCode+amount+category), always-append (never an update), status always starts 'Pending'. Ungated except the ESS self-scope check (ported in the TS gateway wrapper, matching the established convention from the KRA write-RPCs -- auth/self-scoping lives in TS, not SQL). Client API_ROUTES capability entry added (purely additive, ships dark). Full detail: decision_log/2026.08.30_HRMS_v716_reimbursement-submit-writerpc_v1.md. */ /* v715 (2026-08-30): Sheets-Independence campaign, NOT the Sentry bugfix work order -- TOCTOU-race retrofit for 3 older write-RPCs (gw_compoff_approve/gw_kra_self_rating_submit/gw_kra_mgr_rating_submit), Supabase-only (SQL + gateway TS), NO HTML/GAS functional change. Bumped for 4-way version-lockstep parity only. Full detail: decision_log/2026.08.30_HRMS_v715_toctou-retrofit_v1.md. */ /* v714 (2026-08-30): Sentry bug-fix sprint, Bug 2 -- admin cleanup-tool ("Clean up go-live auto-UL", action cleanupGoLiveAutoUL) null-check guard. Root cause investigation: Sentry showed TypeError: Cannot read properties of null (reading 'value') at document.getElementById('v121cl-from').value inside _v121Run, release v600, hrms_role: owner, unhandled/High/Ongoing. Compared against the archived v600 source (Archived Versions/v600-v699) byte-for-byte at this call site: the element id and the collapsed "#dfx-onetime-wrap" (display:none, NOT removed from DOM) structure are IDENTICAL between v600 and v712 -- rules out an ID rename/removal (hypothesis b) and rules out a code path that skips rendering these fields entirely (hypothesis c), since the fields are unconditionally part of the same modal body string in both versions. The remaining, most plausible explanation is a timing/lifecycle race (hypothesis a): a stale/delayed click event firing _v121Run AFTER the Data Fixes modal itself had already been closed (its DOM content removed), which the code never guarded against -- not fully proven (this could not be live-reproduced), but the strongest identified candidate. FIX (narrow, defensive-only, per Rule 8/14 HIGH CAUTION since this tool writes to the live sheet): added an existence check on both #v121cl-from and #v121cl-to before reading .value, with a clear user-facing toast telling the owner to reopen the panel. Does NOT touch params.dryRun, params.apply, the confirm2() "this writes to the live sheet" dialog, or the actual API.call(action, params) write itself -- all preserved byte-for-byte. HTML-only; GAS bumped for version-parity only, no functional change. Full detail: decision_log/2026.08.30_HRMS_sentry-bugfix-sprint_v1.md. */ /* v713 (2026-08-30): Sentry bug-fix sprint, Bug 1 -- kiosk face-scan crash guard. Root cause: faceDetectLoop()'s async IIFE wrapped its identify-chain call in a bare try{}finally{} with NO catch, so any exception from the identify chain became an UNHANDLED promise rejection (Sentry: IndexSizeError inside face-api.js getImageData, mechanism=onunhandledrejection, release v628, kiosk clock-in). All existing literal getImageData call sites (_v332ShotQuality, _v340AntiSpoofCheck, _v328Identify) already guard videoWidth>0 before touching a canvas, so this is the strongest identified gap, not a fully re-proven root cause (face-api.js's internal canvas work is invisible to a literal source grep, same conclusion as the v642 ESS-side fix). FIX (defensive guard only, does not touch handleClockIn/doClockIn/any attendance-writing function): (1) added an explicit videoWidth>0 && readyState>=2 check at the top of the loop tick so a not-ready camera frame is skipped and retried on the next 400ms self-paced tick, same pattern as _v328Identify's own guard; (2) added a catch(_v713e) clause so ANY residual exception is now HANDLED (console-only, no toast spam) instead of leaking as an unhandled rejection. HTML-only; GAS bumped for version-parity only, no functional change. Full detail: decision_log/2026.08.30_HRMS_sentry-bugfix-sprint_v1.md. */ /* v712 (2026-08-30): Gateway-only, no HTML functional change beyond the 2 new API_ROUTES capability entries below (purely additive) -- rejectLateClockIn + rejectRegularisation write-RPCs, the safe half of the 4 approve/reject actions deferred at v710 (the 2 APPROVE actions need porting the full handleClockIn/handleManualSetAttendance core attendance engines, ~281+113 lines per an audit-corrected citation, left as a future project -- decision_log/2026.08.30_HRMS_lateapprovals-regularisations-approve-reject-design_v1.md). Ships fully dark: neither Late_Approvals nor Regularisations is EDGE_OWNED, API_ROUTES (server-side) unflipped. Full detail: decision_log/2026.08.30_HRMS_v712_lateapprovals-regularisations-reject-writerpc_v1.md. Prior: v711 (2026-08-29): Gateway/GAS-only, no HTML functional change -- new clearFaceDescriptors write-RPC (Employees), owner-token-gated only, ships fully dark. Originally scoped as a v670 design-audit build requirement, never actually built until now. Closes the gap where a Sheet-only "Clear Face Data" left a stale real faceDescriptor sitting in the pre-existing Postgres mirror indefinitely. New SQL function not yet applied (classifier-blocked for Claude Code, queued for the owner's manual Supabase apply, same as several prior versions this campaign). Client API_ROUTES capability entry added (purely additive). Full detail: decision_log/2026.08.29_HRMS_v711_clearfacedescriptors-writerpc_v1.md. Prior: v710 (2026-08-29): Late_Approvals + Regularisations promoted from zero Postgres presence to structured tables (migration applied live: both created, 0 rows, awaiting first periodic/backfill tick) + periodic-mirror + daily-reconcile-watchdog coverage from day one, backing 2 new dark edge reads (getLateApprovals/getRegularisations, both ungated, ported exactly from their existing GAS handlers -- zero calc-engine calls in either). The 4 approve/reject WRITE actions on these 2 tables are deliberately NOT built this version -- one carries real documented P0-money-bug history (v651), needs its own dedicated design pass. Ships fully dark: client capability entries added (purely additive), no route flip, no EDGE_OWNED_TABLES change. Full detail: decision_log/2026.08.29_HRMS_v710_lateapprovals-regularisations-promotion_v1.md. Prior: v709 (2026-08-29): GAS-only, no HTML/gateway functional change -- reconcile-watchdog coverage extended to CompOff/TnC_Clauses/Notifications/KRA_Assessments (owner-screenshot-confirmed real Sheet data missing from Postgres for TnC_Clauses; same v684 bug class). Full detail: decision_log/2026.08.29_HRMS_v709_reconcile-watchdog-coverage-fix_v1.md. Prior: v708 (2026-08-29): Gateway+HTML-only, no GAS functional change -- getSalaryPaymentStatus dark read + bulkSetSalaryPaymentStatus write-RPC, the two natural follow-ons to v707's setSalaryPaymentStatus, deferred at that version's own research fork. Both GAS handlers being ported already existed unchanged since v358 -- this version only adds their Postgres/gateway equivalents (new gw_salary_payment_status_bulk_set() SQL function that calls v707's already-audited gw_salary_payment_status_set() once per item, guaranteeing byte-identical per-row behavior and preserving GAS's real per-item continue-on-failure tally for free). getSalaryPaymentStatus is module-gated only ('payroll', reused); bulkSetSalaryPaymentStatus is DUAL-gated ('payroll' + owner-token), matching handleBulkSetSalaryPaymentStatus's real _checkOwnerToken call exactly (confirmed directly against source, the same owner-token-only-vs-dual-gated mistake v707 caught was checked here specifically to avoid repeating it). Ships dark on both layers. Full detail: decision_log/2026.08.29_HRMS_v708_salarypaymentstatus-read-bulkwrite_v1.md. Prior: v707 (2026-08-29): Gateway+GAS+HTML -- setSalaryPaymentStatus write-RPC. Salary_Payment_Status promoted this same version from the once-daily blob mirror (added v635, never further promoted) to a real structured table + periodic mirror + instant-mirror-on-write (composite Employee Code, Year, Month key synthesis, matching the Leave_Balance precedent), plus proactive self-heal reconciliation coverage added at promotion time. Dual-gated, matching GAS exactly -- an earlier draft's owner-token-only claim was caught false by both independent audits, GAS's real dispatch also has a payroll module gate; fixed by adding it to the gateway. Payment-STATUS ledger, not a payroll computation input -- never calls calcMonthlySalary. GAS's real amountPaid-defaulting quirk and round-to-paise amountPending formula both replicated exactly. A robustness gap self-caught before any audit: a bare numeric cast on garbage input would have crashed the RPC instead of gracefully defaulting like GAS's parseFloat does -- fixed with a safe try or catch parse pattern for both amount fields. Ships dark. Full detail: decision_log/2026.08.29_HRMS_v707_salarypaymentstatus-writerpc_v1.md. Prior: v706 (2026-08-29): Gateway+HTML-only -- updateMilestone write-RPC. Genuine upsert-by-key on Milestones (real PK on years, confirmed live). Open route, independently re-confirmed via grep directly on the real GAS dispatch (no ACTION_MODULE, no _checkOwnerToken). Replicates the active: p.active!==false JS-boolean quirk (only the literal boolean false triggers false, incl. the STRING 'false' triggering true) as a jsonb-value comparison, not text; years/reward/description are the only 3 fields with no OR default in GAS's real patch object, given the jsonb-existence-check (?) preserve-if-absent treatment already established for the KRA write-RPCs (v702/v703) -- the other 10 fields always overwrite, matching GAS's own merge-guard exactly. Ships dark. Full detail: decision_log/2026.08.29_HRMS_v706_updatemilestone-writerpc_v1.md. Prior: v705 (2026-08-29): Gateway+HTML-only -- approveCompOff write-RPC, module-gated only, matching GAS exactly. Ships dark. Full detail: decision_log/2026.08.29_HRMS_v705_approvecompoff-writerpc_v1.md. Prior: v704 (2026-08-29): HTML-only -- closes the admin Pulse aggregate panel bug disclosed at v692 (client read a response shape that never existed). Zero backend change. Full detail: decision_log/2026.08.29_HRMS_v704_pulseaggregate-uifix_v1.md. Prior: v703 (2026-08-29): Gateway+HTML-only -- submitKRAMgrRating write-RPC: manager ratings + relationship check, matching GAS exactly. Finalization engine NOT ported (disclosed hard gap). Ships dark. Full detail: decision_log/2026.08.29_HRMS_v703_kramgrrating-writerpc_v1.md. Prior: v702 (2026-08-29): Gateway+HTML-only -- submitKRASelfRating write-RPC ONLY (companion mgr-rating deferred to v703). Open route, self-scoped, matching GAS exactly. Ships dark. Full detail: decision_log/2026.08.29_HRMS_v702_kraselfrating-writerpc_v1.md. Prior: v701 (2026-08-29): Gateway+HTML-only -- Holidays write-RPC bundle, module-gated only, matching GAS exactly. Ships dark. Full detail: decision_log/2026.08.29_HRMS_v701_holidays-writerpcs_v1.md. Prior: v700 (2026-08-29): Gateway+HTML-only -- addOnboardingTask + completeOnboardingTask write-RPCs, dual-gated, matching GAS exactly. Ships dark. Full detail: decision_log/2026.08.29_HRMS_v700_onboarding-writerpcs_v1.md. Prior: v699 (2026-08-29): Gateway+HTML-only -- upsertPOSHICC write-RPC, dual-gated, matching GAS exactly. Bundled audit-fix: addDocument (v695) was missing from WRITE_TABLE's ownership guard, now fixed. Ships dark. Full detail: decision_log/2026.08.29_HRMS_v699_poshicc-writerpc_v1.md. Prior: v698 (2026-08-29): Gateway+HTML-only -- money-tier computed-value read getCompOff, module-gated only, matching GAS exactly. Safety argument re-verified against source. Ships dark. Full detail: decision_log/2026.08.29_HRMS_v698_compoff-read_v1.md. Prior: v697 (2026-08-29): Gateway+HTML-only -- money-tier reads getBonusEligibility + getLoyaltyBonusAllBalances, module-gated only, matching GAS exactly. Ships dark. Full detail: decision_log/2026.08.29_HRMS_v697_bonuseligibility-loyaltybalances-reads_v1.md. Prior: v696 (2026-08-29): GAS+Supabase(hrms-db) -- closes v695's disclosed gap: addDocument's dark write-RPC had no read path. handleDownloadDocument/handleGetDocuments/handleDeleteDocument fall back to Postgres (2 new hrms-db ops) only when a doc isn't found in the Sheet -- zero change to existing Drive-backed documents' behavior. Config-gated merge (DOCUMENTS_SUPABASE_MERGE, default OFF). Still not flipped live. Full detail: decision_log/2026.08.29_HRMS_v696_adddocument-readpath_v1.md. Prior: v695 (2026-08-29): GAS+Gateway -- Documents promoted to structured periodic mirror (GAS), + a dark, unflipped addDocument write-RPC porting NEW document uploads to Supabase Storage instead of Drive (Handoff v45.0 Part B item 1, Option 3; existing Drive documents untouched). Read path for Supabase-stored documents NOT built yet -- disclosed hard blocker on ever flipping this route. Full detail: decision_log/2026.08.29_HRMS_v695_documents-structured-mirror-and-adddocument-writerpc_v1.md. Prior: v694 (2026-08-28): Gateway-only, no GAS functional change -- MONEY-TIER write RPC (build + read-only parity only, Rule 14): addPoints -> Points via a new gw_points_upsert() SQL function, not yet applied (owner deploy pending). Ships dark on two independent layers (API_ROUTES unflipped + Points not edge-owned). Full detail: decision_log/2026.08.28_HRMS_v694_addpoints-writerpc-design_v1.md. Prior: v693 (2026-08-28): Gateway-only, no GAS functional change -- non-money batch, getTnCClauses (admin clause-editor) dark read, owner-token-gated only (matching GAS exactly). Pure read+filter+sort, no cross-table staleness dependency, unlike getPettyCash/getNoticeRecords (both investigated and declined this session). Full detail: decision_log/2026.08.28_HRMS_v693_tncclauses-dark-read-design_v1.md. Prior: v692 (2026-08-28): Gateway-only, no GAS functional change -- non-money batch, getActivePulseQuestion + getPulseAggregate (Pulse survey) dark reads, both tables already instant-mirrored since v615. Two real, pre-existing, unrelated bugs found and disclosed (not fixed, Rule 1): the real ESS Pulse button is broken for true ESS-home-portal sessions (missing from GAS's own _ESS_READ_ALLOW), and the admin Pulse aggregate panel reads response fields that don't exist on the real GAS response shape, so it never shows real aggregate results. Full detail: decision_log/2026.08.28_HRMS_v692_pulse-dark-reads-design_v1.md. Prior: /* v690 (2026-08-28): Gateway-only fix -- getNotifications limit-parsing parity (v688 Audit A LOW finding): GAS's Math.min(parseInt(p.limit||50,10),200) yields NaN on non-numeric input -> 0 rows; gateway's earlier `|| 50` fallback after parseInt silently returned up to 50 instead. Dropped the fallback so NaN propagates identically. No HTML functional change. Full detail: decision_log/2026.08.28_HRMS_v690_notifications-limit-parity-fix-design_v1.md. Prior: v689 (2026-08-28): GAS-only reconciliation-coverage follow-up (OT_Claims + PettyCash added to the v684 self-heal array), no HTML functional change. Full detail: decision_log/2026.08.28_HRMS_v689_otclaims-pettycash-reconcile-coverage-design_v1.md. Prior: v688 (2026-08-28): non-money batch -- Milestones + Notifications dedicated edge reads, gateway-only (see gw21/v688's own comment for the full story). No GAS change this round. Full detail: decision_log/2026.08.28_HRMS_v688_milestones-notifications-reads-design_v1.md. Prior: v687 (2026-08-28): Money-Tier Wave 9 -- OT_Claims dedicated edge read (getOTClaims), gateway-only handler (see gw20/v687's own comment for the full story, incl. the client-critical _rowIndex re-derivation) plus this one client-side addition: getOTClaims added to the API_ROUTES capability list so a future flip actually works. No GAS change this round. Full detail: decision_log/2026.08.28_HRMS_v687_getotclaims-edge-read-design_v1.md. Prior: v685 (2026-08-28): diagnostic-only fix, no functional change. LoadingOverlay.forceReset()'s console.warn fired unconditionally, including from the v611 nav-safety call that runs on EVERY screen navigation by design -- so it logged on every tab switch regardless of whether anything was actually stuck, and had been mistaken for a symptom of the v492 freeze-bug family across multiple audit sessions (it is not; that real race was already fixed at v614's discarded-hide-call counter, unchanged here). Now only warns when there was genuinely something to reset. Zero change to the actual state-clearing logic; confirmed this log string has no other consumer anywhere in the file. Full detail: decision_log/2026.08.28_HRMS_v685_forcereset-diagnostic-fix_v1.md. Prior: v684 (2026-08-28): HIGH-severity fix -- extends the existing v641 self-heal reconciliation pattern to 6 periodic-only mirror tables (Leave_Balance, PT_Slabs, Leave_Encashment, Leaves, SalaryRecords, Transactions) plus wires the same self-heal into Attendance's own separate dedicated job, closing zero-protection gaps proven live: Leave_Balance's mirror had been empty for 14 days. Went through 2 independent code audits: one HIGH concern (a 1000-row read cap) was disproven against the live Edge Function source (already fixed since 2026-08-02); 2 real issues from the other (no time-budget on the reconciliation loop; a double-reconciliation risk for Attendance) were both fixed before shipping. GAS-only, zero HTML functional change this round. Full detail: decision_log/2026.08.28_HRMS_v684_moneytier-reconciliation-coverage-design_v1.md. Prior: v683 (2026-08-28): SHEETS-INDEPENDENCE CAMPAIGN, Money-Tier Wave 9 -- PettyCash + OT_Claims instant-mirror completion -- GAS-only, zero HTML functional change this round (10 write paths across 2 tables gained an instant Postgres mirror on top of their existing periodic tier; 2 design audits ran clean, no blocking defects). Full detail: decision_log/2026.08.28_HRMS_pettycash-otclaims-synthesis_v1.md / _v683_pettycash-otclaims-mirrors-design_v1_synthesis.md / _v2_audit-resolutions.md. Prior: v682 (2026-08-27): SHEETS-INDEPENDENCE CAMPAIGN, Attendance/Leaves instant-mirror completion batch -- GAS-only, zero HTML functional change this round. Full detail: decision_log/2026.08.27_HRMS_attendance-edits-batch-synthesis_v1.md / _v682_attendance-mirror-fixes-design_v1_synthesis.md / _v2_audit-resolutions.md. Prior: v681 (2026-08-27): SHEETS-INDEPENDENCE CAMPAIGN, Money-Tier Wave 8 -- Transactions read-mirror. New gateway action getTransactions ships dark, ACTION_MODULE:'transactions' gated (matches GAS exactly; genuinely looser than getSalaryRecords's payroll gate, but that's because Transactions is already a more open module in production today). Real work this round is GAS-side: added an instant Postgres mirror to 9 real write paths across 11 edit sites (Transactions previously only had the ~5-min periodic mirror, no write path fired instantly) -- 2 independent design audits caught and fixed 2 real defects before any code was written (a wrong-id mirror-row bug; 2 more real write paths the original freshness scan had missed). Client-side API_ROUTES gained the one new capability entry. Full detail: decision_log/2026.08.27_HRMS_v681_moneytier-transactions-design_v1_synthesis.md / _v2_audit-resolutions.md. Prior: v680 (2026-08-27): SHEETS-INDEPENDENCE CAMPAIGN, client-routing-sync fix (no new gateway action, no route flip). A code audit for v679 found this file's own API_ROUTES capability list (the client-side gate that must ALSO say 'edge' before a server-side Gateway_Control flip has any effect) had not been updated since v669 -- every money-tier/edge action built since (getLeaveEncashmentRequests, saveQrToken/saveNfcId/getEmployeeBadges, getBonuses, the 6 v675 write RPCs, getLeaveBalance, getLeaves, getAttendanceEmployee, getSalaryRecords -- 15 actions total) was genuinely absent, meaning a future server-side flip of any of them via Settings would have silently done nothing client-side. Fixed by adding all 15 as capability entries -- this is PURELY ADDITIVE and changes NOTHING today: Gateway_Control.API_ROUTES (server-side) is still '{}' (nothing flipped), so every one of these still falls through to Google exactly as before this fix. No GAS changes this round. Full detail: decision_log/2026.08.27_HRMS_v680_client-routing-sync_v1.md. Prior: v679 (2026-08-27): SHEETS-INDEPENDENCE CAMPAIGN, Money-Tier Wave 7 -- SalaryRecords dedicated edge read, the highest-stakes table this campaign has approached. New gateway action getSalaryRecords ships dark, ACTION_MODULE:'payroll' gated (stricter than any prior wave). Confirmed to never call the real payroll calculation engine. Bundled, disclosed schema migration (structure only, no backfill) applied live to Postgres. Zero GAS functional changes this round. Full detail: decision_log/2026.08.27_HRMS_v679_moneytier-salaryrecords-design_v1_synthesis.md / _v2_audit-resolutions.md. Prior: v678 (2026-08-27): SHEETS-INDEPENDENCE CAMPAIGN, Money-Tier Wave 6 -- Attendance dedicated edge read. New gateway action getAttendanceEmployee ships dark (no route flip, no ESS_ALLOW/ACTION_MODULE entry, matching GAS's real ungated/zero-ESS-reach posture exactly). Zero GAS functional changes this round. Full detail: decision_log/2026.08.27_HRMS_v678_moneytier-attendance-design_v1_synthesis.md / _v2_audit-resolutions.md. Prior: v677 (2026-08-27): SHEETS-INDEPENDENCE CAMPAIGN, Money-Tier Wave 5 -- Leaves dedicated edge read. New gateway action getLeaves ships dark (no route flip, no ESS_ALLOW/ACTION_MODULE entry, matching GAS's real ungated-for-admin/zero-ESS-reach posture exactly). One isolated, disclosed GAS-side addendum found via this build's own research: handleBulkDeleteAutoULLeaves's sole Leaves mirror call was firing while its ScriptLock was still held -- reordered to fire after release (zero data-scope change). Full detail: decision_log/2026.08.27_HRMS_v677_moneytier-leaves-design_v1_synthesis.md / _v2_audit-resolutions.md. Prior: v676 (2026-08-26): SHEETS-INDEPENDENCE CAMPAIGN, Money-Tier Wave 4 -- Leave_Balance read-mirror. New gateway action getLeaveBalance ships dark (no route flip); GAS side gained a bundled unrelated security fix (getLeaveBalances plural, a pre-existing unscoped whole-table-dump action with zero real callers, now owner-token-gated). No HTML/frontend UI logic changed this round -- CACHE key bump is 4-way version-lockstep housekeeping only. See decision_log/2026.08.26_HRMS_v676_moneytier-leavebalance-design_v1_synthesis.md / _v2_audit-resolutions.md. Prior: v667 (2026-08-25): SHEETS-INDEPENDENCE CAMPAIGN, WAVE 2 BATCH 1. Seven more actions become edge-capable (dark until the owner flips them): 1:1 notes (getOneOnOneNotes, addOneOnOneNote), Activities/Projects/Clients settings (getActivities, addActivity, updateActivity), and two fast reads whose Sheets stay the record (getPOSHICC, getOnboardingChecklist). Kept on Google by design: POSH complaint filing/reading (the confidentiality passphrase must never leave Google), and three writes that need a proper record key first (POSH committee members, onboarding tasks). No pay/leave/attendance calculation touched anywhere. Prior: v666 (2026-08-25): SHEETS-INDEPENDENCE CAMPAIGN, WAVE 1 BATCH 2. Recruitment's addCandidate and getCandidates become edge-capable (dark until the owner flips them). Three handlers are formally classified as staying on Google by design -- generateOfferLetter (Google Drive), sendOfferEmail (Gmail), acceptEssTerms (signature image, owner decision pending) -- and the four Pulse-survey handlers are on hold for an owner decision (the survey cannot be answered on any road today: the form and the server disagree on field names and the action is not on the employee allow-list). No pay/leave/attendance calculation touched anywhere. Prior: v665 (2026-08-25): SHEETS-INDEPENDENCE CAMPAIGN, WAVE 1 BATCH 1. (a) Route flips move SERVER-SIDE: the routing table in this file is now only the list of what the fast server CAN do; which actions actually use it is decided by the owner after deploy, per action, from Settings -> Speed & Health (stored in the database as Gateway_Control.API_ROUTES and enforced by the fast server itself on every call -- a stale tab cannot bypass a flip). Every route ships DARK: on the day this version is deployed the app behaves exactly like v663 until the owner flips something. (b) Ten more actions become edge-capable: Roster (getRoster, getRosterMonth, updateRosterBatch), Assets (getAssets, getMyAssets, getUnreturnedAssetsForEmp, upsertAsset, returnAsset), KRA templates (getKRATemplates, upsertKRATemplate). (c) The Danger-Zone token travels to the fast server as a separate envelope field so owner-token-gated actions can run there (verified against a hash the server publishes; unverifiable == the slow road decides). (d) The Speed card counts the campaign's reconciled 229 handlers and gains per-action flip buttons. No pay/leave/attendance calculation touched anywhere. Prior: v664 (2026-08-25): EDGE GATEWAY FOUNDATION (Handoff v40.0 Part 2). The app's single API.call() choke-point gained an API_ROUTES routing table: an action marked 'edge' is sent to the new hrms-gateway Supabase Edge Function (JWT-verified, role-gated, idempotent, transactional, audited) instead of Google Apps Script; every other action is byte-identical to before. First four actions flipped: getTickets/updateTicket/addTicket/getMyTickets (Helpdesk -- lowest-stakes Tier 3a table). Per-endpoint rollback = flip the row back to 'gas'; owner kill switch (Settings -> Speed & Health) forces every device back to Google in one click; a per-device pause exists too. A failed Edge call LOOKS like a failure (same _v496Lost honesty path). Latency harness: every call's wall time (edge and Google) is kept per action on this device and shown as p50/p95 on the Speed & Health card, baselined BEFORE any flip. GAS v664: Tickets becomes 'edge-owned' (Sheet->Postgres mirror skipped for it; GAS-path writes forward their single row to Postgres; reads come from Postgres; a new hourly edgeExportTick writes Postgres -> the Tickets Sheet as a backup and appends Edge audit rows to the Audit Sheet). No pay/leave/attendance calculation touched anywhere. Prior: v663 (2026-08-24): GAS-only fix -- TWO Date-typed-cell bugs (4th+5th occurrence this month of the same class as v649/v650), both found live while closing the phantom-OT incident. FIX 1: handleVoidPhantomOTBackfill662's own fingerprint check compared String(dateCell).slice(0,10) against '2026-08-22', but the Attendance date cell round-trips as a real JS Date object, so String(date) never produces an ISO-prefixed string -- the tool aborted 13/13 every run (caught live when the owner clicked it). FIX 2: handleManualSetAttendance's (the Time Adjustment tool) update-path workMinutes recompute mangled a Sheet-read Date object the same way whenever an edit left clockIn/clockOut untouched -- silently NaN, workMinutes never set; this is the exact site behind the 2026-08-22 recovery silently computing 0 minutes for Vivek Kumar. Both fixed using this codebase's own established instanceof-Date-safe normalizer (same pattern as ~6313/6562/6596/8408); handleManualClockIn's similar-looking site was checked and confirmed NOT vulnerable (always a fresh string, never a Sheet read). No HTML/frontend logic changed -- CACHE key bump is version-lockstep housekeeping only. Prior: v662 (2026-08-24): HTML+GAS fix -- 2026-08-24 phantom-OT-backfill incident (owner's manual attendance recovery, done via the Bulk Attendance Excel importer, included a stray session='OT' + placeholder 19:00-to-02:00/03:00 clock times for 13 employees' 2026-08-22 row inside the uploaded file itself -- confirmed by code-elimination against the live Supabase mirror, NOT a defect in the importer, which faithfully wrote what the file contained; see decision_log/2026.08.24_HRMS_v662_phantom-ot-cleanup-and-guard_v1.md). Ships two things: (1) a new owner-gated, server-side-hardcoded-allowlist one-time correction (handleVoidPhantomOTBackfill662 / _v662RunPhantomOTCleanup, under Settings -> Backfill Imports) that re-verifies each of the 13 rows against its exact confirmed fingerprint immediately before zeroing it -- aborts the whole batch untouched if even one row no longer matches, idempotent on re-run; (2) a loud pre-commit callout in bfCommitAttendance listing every OT/second-session row in a staged batch (empId/date/clock-in-out) before it can be written, so the same silent-typo failure mode is caught before commit instead of after. Prior: v661 (2026-08-23): HTML-only fix, found live while verifying v660 -- the kiosk's OPTIMISTIC punch path (_v92IdbDrainQueue, used for nearly every real online punch) showed a false "Clocked in" success message and silently, invisibly kept retrying forever when the server actually held the punch for a legitimate reason (late-approval queue, missed-clockout block, monthly-late cap, owner-locked clock-out) -- live-reproduced: a late test punch showed CLOCKED IN in the kiosk while the server correctly queued it to Late_Approvals instead of writing Attendance, with zero corrective signal to anyone. Mirrors the exact terminal-outcome-detection pattern the sibling GENUINELY-offline replay path (_v258ReplayPinPunch) already used for this same class of response. GAS untouched this round -- CACHE/version bump is 4-way lockstep housekeeping only. Prior: v660 (2026-08-23): Backend-only fix (GAS drainPunchQueue now flushes before telling Supabase a punch is durable -- see gas_backend_v660 for the full root-cause of the 2026-08-22/23 silent attendance-loss incident); ZERO HTML/frontend behaviour changed, this file is re-versioned purely for 4-way lockstep. Prior: v659 (2026-08-22): Infrastructure-only checkpoint, zero app-code change. Built and deployed a brand-new, narrow, purpose-built Postgres view (public.employees_idcard_v -- employees_roster_v's exact 36 safe columns plus ONLY gender and mobile, still zero salary/bank/PIN/biometric/Aadhaar data) per explicit owner approval, and added it to the deployed hrms-read Edge Function's allow-list, OWNER_GATED (v38->v39 of that function). Investigated wiring it into Bulk ID Cards (_v341BulkIdCards) as originally planned, and concluded NOT to: qrToken (the scan-to-clock-in credential) is never mirrored to Postgres anywhere and never will be without separate authorization, and checking/minting it safely (never regenerating an existing token, which would invalidate a previously-printed card) fundamentally requires one full GAS round-trip per employee regardless of what other fields come from Postgres -- so replacing the per-employee getEmployee call with a map lookup would save nothing real, since the same per-employee GAS call is still needed for qrToken. Forcing a partial integration that doesn't meaningfully speed anything up was judged not worth the added complexity -- same "don't build what doesn't actually help" discipline already applied to Employee list/export and the Reports fnf/employees types. The view remains live, deployed, and available for a future round if qrToken mirroring (or a narrower dedicated qrToken-only lookup) is separately scoped and approved. Also recording, for full transparency: this round's Edge Function deploy call was made incorrectly on the first attempt (a placeholder value was sent as the function body instead of the real source, and verify_jwt was left at its default of true instead of the required false) -- caught within the same turn via a direct metadata check, corrected immediately with the real source and verify_jwt explicitly false, and verified healthy via a live HTTP call against the real deployed endpoint (a bogus token correctly returned the function's own {"ok":false,"error":"unauthorized"} at HTTP 401, not a platform-level rejection) before considering the fix verified -- the same live-verification discipline this function's own v33 history already established for this exact failure class. No pay/leave/attendance calculation touched anywhere in this round; no HTML/GAS application logic changed at all. Prior: v658 (2026-08-22): Tickets/Assets Tier 3a Step 3 groundwork -- same conservative "mirror-confirmed signal, no write-authority flip" pattern already proven for Roster at v655. _v587InstantMirrorTickets/_v587InstantMirrorAssets now RETURN their result instead of discarding it; handleAddTicket/handleUpdateTicket/handleUpsertAsset/handleReturnAsset attach a new ticketMirrorConfirmed/assetMirrorConfirmed boolean to their responses. Client-side, _v548TicketsFast/_v544AssetsFast gained the same forced-slow-reread-after-save guard Roster already has (_v658SkipTicketsFast/_v658SkipAssetsFast), consumed only on genuine doubt -- any confirmed mirror success skips the forced GAS round-trip, matching Roster's exact safety margin. Separately, fixed a real risk the Assets design-audit found: the live Add-Asset UI always self-generates its assetId as 'AS_'+Date.now().toString(36) (the server's own-mint fallback never fires through the real UI), which collides within the same millisecond -- switched to a real UUID (crypto.randomUUID, already used elsewhere in this file), collapsing the collision window to negligible; a genuine atomic Postgres ON CONFLICT guard stays deferred to the real cutover, not built as a bespoke Sheets-side lock for a race this narrow. Sheets remains the sole authoritative write for both tables, completely unchanged -- Step 5 (live demo smoke test) and Step 6-7 (any actual authority flip) remain blocked on deploy + a real reconciliation observation window, neither of which this round can produce. No pay/leave/attendance calculation touched anywhere in this round. Prior: v657 (2026-08-22): Promotion Eligibility unified onto the review-queue's stricter rule (owner decision) -- getPromotionEligibility now uses the SAME tenure-start chain (lastLevelChangeDate->incrementCycleStartDate->doj floored by incrementPolicyStartDate, not PROMO_SCHEME_START) and the SAME eligibility test (engine-on AND monthsAtLevel>=minM AND cyclesCompleted>=maxCycles, or 'overdue' if monthsAtLevel>=maxM) as getPromotionReviewQueue, including its L6-exclusion and active-promotionDeferredUntil exclusion -- real, visible consequence: an employee who showed "eligible" under the old looser tenure-only test may now show "not yet eligible" if the increment engine is off for them or their cycle count hasn't caught up; that is the intended effect of unifying onto the stricter rule, not a bug. Loyalty Wallet: a full trace of every LoyaltyBonusLedger-mutating path (forfeiture/restoration/accrual/withdrawal-approval) confirmed each one already re-persists the LoyaltyBonusBalances snapshot after writing, proving the read-time recompute inside handleGetEmployeeLoyaltyBonus/handleGetESSData was redundant re-persistence, not a freshness guarantee -- _updateLoyaltyBonusBalancesSnapshot split into a pure _computeLoyaltyBonusSnapshot (no write) + a thin persist wrapper (unchanged for every write-time caller); both read call sites now use the compute-only path, returning byte-identical data with no Sheets write+flush on every view. The one narrow, already-existing failure mode this trade-off relies on (a per-employee post-accrual snapshot silently failing) is no longer silent -- runMonthlyLoyaltyBonusAccrualInternal now writes a real LB_SNAPSHOT_FAIL Audit row instead of only a GAS Logger.log line. Also: the Employees screen's "Bulk ID Cards" button was found ungated (rendered for every role, unlike its sibling admin buttons) -- now gated to owner/manager like its neighbors, ahead of the new owner-gated employees_idcard_v Postgres view this same button will read from once deployed. No pay/leave/attendance calculation touched anywhere in this round. Prior: v656 (2026-08-22): Phase 1 read-mirror completion batch (safe, non-owner-sign-off items + owner-signed-off Reports subset). Points leaderboard (renderPoints general load + Reports "points" type) fast-pathed via new _v656LeaderboardFast, reproducing handleGetLeaderboard's ranking logic verbatim (eligible-level filter, cross-month week overlap, zero-guard, standard-competition tie-ranking) rather than re-deriving it -- deliberately re-filters status==='Active' strictly rather than trusting _v516EmployeesFast's own looser default (which also includes Notice Period), a real discrepancy caught before it shipped. The two post-save leaderboard refreshes (savePoints/saveAllPoints) were deliberately LEFT on the slow GAS path -- this codebase's own existing comment there ("never render from local state") already establishes a guaranteed-fresh-after-my-own-write intent, matching the same conservative principle this project used for Roster. Bonus Tracker tab got two new wrappers (_v656LBBalancesFast/_v656LBPendingFast) following the proven _v578LBLedgerFast idiom exactly. Reports tab got fast-reads for attendance/leaves/transactions/points/payroll (5 of 7 types, via a new _v656ReportFast dispatcher mirroring handleGetReport's own per-type filter logic verbatim) -- fnf/employees types were investigated and deliberately NOT built: their redaction is genuine server-side _fieldVisStrip logic against the raw Employees table, and that raw table (verified directly against the live deployed hrms-read Edge Function source) is not even in its allow-list today -- adding it would be a live security-boundary change exposing the single most sensitive table in the system (raw login PIN, biometric face descriptor, Aadhaar, full bank details), which this session chose not to make unilaterally without the owner explicitly seeing it first. Bulk Employee ID/QR card generation and the full Employee list/detail/export screen were both scoped and found NOT buildable as originally proposed (missing fields / no safe data source respectively, see docs/Postgres_Migration/Phase1_ReadMirror_Completion_Backlog_v1.1_2026-08-22.md for the full reasoning) -- deferred, not forced. Loyalty Wallet/Joining Bonus and Promotion Eligibility got proper design-note treatment instead of code (see the two matching docs under docs/Postgres_Migration/) -- the former needs a real backend read/write split before any client change, the latter surfaced a real, pre-existing, unrelated bug (the two eligibility screens disagree on tenure formula/thresholds) that was flagged to the owner rather than silently touched. Tickets/Assets Tier 3a designs got their mandatory Step 2 (2 independent audit passes each, run this round) -- both design docs bumped to v1.1 with the corrections found (Assets' duplicate-ID race is the NORMAL path not a rare edge case; a pre-existing horizontal-access gap in handleGetAssets flagged separately for the owner). No pay/leave/attendance calculation touched anywhere in this round -- every change is either a read-side speed optimization with fallback-on-any-doubt, or a design/audit document. Prior: v655 (2026-08-22): Handoff v33.0 Roster Step-3 write-cutover groundwork. Closed the _clearRosterCellsForLeave Postgres-mirror gap the v654 audit had documented-but-deferred (never told the Roster Postgres mirror on a leave cancel/revert/edit/delete's roster-cell clear) -- NOT by adding the mirror call inside that shared chokepoint (still holds the app-wide LockService lock there, still the correctly-reverted risk from v654's audit), but via a module-level dirty flag (_v655RosterMirrorDirty) that each top-level lock-holding entry point that can reach it checks-and-clears AFTER its own lock.releaseLock(), firing the same proven _v587InstantMirrorRoster() only when a cell was actually touched. An initial pass found only 6 such entry points (handleCancelLeave, handleRevertLeave, handleAdminCancelLeave, handleBulkApproveLeaves, handleEditLeave, handleCancelLeaveRequest) and shipped incomplete; two independent adversarial audits of this exact change, run in parallel, both independently converged on 5 more that were missed (handleReconcileAttendanceLeave, handleSaveSalaries [the payroll-save handler], handleApproveLateClockIn, handleAttendanceAdjustment, handleBulkAttendanceEntry) -- all 11 are now wired, the 4 more complex/money-adjacent ones via a thin-wrapper/renamed-core pattern rather than editing their internal control flow directly. Also: _v587InstantMirrorRoster() now RETURNS its result instead of discarding it, and handleUpdateRoster/handleUpdateRosterBatch attach a new rosterMirrorConfirmed boolean to their response -- the client's saveRoster()/_v572SkipRosterFast guard (which previously forced a slow authoritative GAS re-read after EVERY roster save, to compensate for the best-effort mirror's silent-failure possibility) now skips that forced round-trip only when this save's own response positively confirms Postgres is fresh; any doubt falls back to the exact same forced-slow-path behavior as before. No write-path primacy flip (Sheets stays the authoritative, lock-gated write for both handlers, unchanged) and no config flag added -- this is a correctness fix (closes a self-healing-but-real gap) plus a narrowly-scoped, provably-safe latency win on the read side, not a cutover of Roster's write authority. Per the Phase 3 Tier 3a design doc's own conclusion, actually flipping which store is authoritative for Roster writes remains blocked on Step 5 (a live demo smoke test), which needs this build deployed first. Prior: v654 (2026-08-22): Handoff v32.0 Phase 1 + Phase 3 groundwork. GAS-side (see the GAS HRMS_VERSION comment for full detail): fixed reconcileTier1DualWrite's false-alert bug (Tickets/Assets), added reconciliation for 15 more instant-mirrored tables (Attendance zero-tolerance + 14-table extended, closing this project's "no observation signal" gap), fixed 2 real pre-existing handleUpdateRosterBatch duplicate-key bugs (a 2nd audit pass caught the first fix was incomplete), and tried-then-REVERTED an instant-mirror call in _clearRosterCellsForLeave after the 2nd audit pass found it would hold a live network call inside the app-wide LockService lock (left as a documented gap, not shipped risky). Also hardened all 3 new reconciliation jobs' pk-lookup (throws instead of silently guessing 'id' if ever unresolvable) and widened their daily trigger spacing (01:45/02:15/02:45, was 01:45/01:50/01:55) per a 3rd independent code audit. Design docs also written for Tickets/Assets Tier 3a cutover (Step 1 only, no code) -- see docs/Postgres_Migration/. HTML-side this round: (1) Settings tab's initial-load read swapped to the existing _v518GetSettingsCached() wrapper (pure oversight fix -- the wrapper already existed and was proven safe at 2 other boot-time call sites, just never wired here). (2) An 8-way parallel module survey (Employee Directory, Attendance, Leaves, Payroll, Performance/KRA, Settings/Admin, Gamification, Reports/Dashboard) catalogued every remaining slow Sheets-read path across the whole app -- see docs/Postgres_Migration/Phase1_ReadMirror_Completion_Backlog_v1.0_2026-08-22.md for the prioritized, owner-sign-off-aware backlog; most money/PII-adjacent findings are deliberately NOT implemented this round pending explicit owner review per rule 8/14, only this one already-safe fix shipped. No pay/leave/attendance calculation touched anywhere in this round. See decision_log/2026.08.22_HRMS_v654_phase1-phase3-buildout_v1.md. Prior: v653 (2026-08-22): Handoff v32.0 Phase 0, GAS-only, non-authoritative-mirror fixes only (Sheets stays the source of truth for both -- no pay/leave/attendance formula touched, no HTML/frontend logic changed this round). Finding 2 -- handleAttendanceAdjustment's hard-delete branch removed rows from the Attendance sheet but never told the Supabase Attendance mirror, leaving ghost rows there until the next full periodic reconciliation self-healed them (confirmed live: 3 real test deletions left ghost rows); added a new targeted _sbDeleteIds_ helper (backed by a new 'deleteIds' op on the hrms-db Edge Function, capped at 500 ids/call) wired into that branch. Finding 1 -- ConfigPublic (the redacted Settings mirror used for a fast Settings load) only ever refreshed as a side effect of a real settings save or the owner's manual repair button, with no periodic backstop unlike every other mirrored table; added a cheap skip-if-unchanged self-heal call into the existing 5-min sbAutoSyncTick. The raw "Config" sheet (secrets) remains correctly and permanently excluded from every Supabase path -- untouched by this fix. No service-worker-relevant change -- CACHE key bump is version-lockstep housekeeping only. See decision_log/2026.08.22_HRMS_v653_phase0-attendance-delete-mirror-and-config-selfheal_v1.md. Prior: v652 (2026-08-22): Task 1, money-adjacent perf fix (GAS-only, no HTML/frontend logic changed this round) -- checkAdvanceEligibility's full unfiltered Attendance-sheet read (live-measured ~28-35s per Advance-type transaction, the actual root cause of the reported "up to a minute" Add Transaction slow-save) was computed unconditionally even though it is only a fallback for when calcMonthlySalary throws. Moved into the catch block (lazy) and swapped for the existing proven _v463GetAttendanceRowsForEmpMonth cached-filter helper. Neither branch's computed value changes; calcMonthlySalary itself untouched. No service-worker-relevant change -- CACHE key bump is version-lockstep housekeeping only. Prior: v651 (2026-08-22): Task 0 [P0 money/data-integrity] -- handleApproveLateClockIn (and the sibling handleApprovePendingAttendance, both clockIn/clockOut branches) never passed the employee's true captured punch time through to handleClockIn/handleClockOut, so the recorded clockIn/clockOut silently became the approval-click moment instead (handleApproveLateClockOut already had this exact fix since v395 -- the missed sibling pattern). Now all pass offlinePunchAt = the true captured time, with an optional owner-edited override from a new inline 12-hour time picker in the Late Clock-In/Clock-Out Approval card (reuses the app's own canonical build12hPicker widget, CLAUDE.md rule 12, rather than a one-off free-text input). Task 1 -- toast() no longer auto-dismisses (owner request: a notification should stay until manually closed); each toast gets its own close button, toasts stack freely with no cap (a deliberate simple choice, not a disguised timeout -- see decision log). Task 2 -- the Danger Zone Override-eligibility-cap box (#at-override-wrap) had 3 text elements relying on colors meant for the app's dark page background against its own hardcoded light-cream box background (WCAG-checked: as low as ~1.4:1 contrast); explicit dark colors added, all re-checked >=8:1 (AA needs 4.5:1). No service-worker-relevant change -- CACHE key bump is version-lockstep housekeeping only. Prior: v650 (2026-08-21, Task 1, GAS-only correctness fix): _v121CancelAutoULLeave had the identical unnormalized date-comparison bug just fixed in handleUndoShiftChange (v649) -- LEAVES.startDate/endDate cells can round-trip as a Date object, mishandled by the old String(cell).slice(0,10) comparison. Called from 4 production attendance-correction sites, all leave-balance-adjacent. Fix: added a local _caulNorm normalizer (same proven pattern as _delNorm/_ushcNorm). No HTML logic touched -- this bump is version-lockstep housekeeping only. Prior: v649 (2026-08-21, Task 0, GAS-only correctness fix): handleUndoShiftChange picked the WRONG shift-history period to delete when a Sheet-stored effectiveFrom cell was a Date object mixed with a plain string -- unnormalized String(cell).slice(0,10) comparison sorted a Date.toString() prefix like "Wed Jun 10" AFTER any "YYYY-MM-DD" string, so it deleted the older row instead of the newer one. Fix: added a local date normalizer (same pattern as handleAttendanceAdjustment's existing _delNorm) and used it in the comparison. No HTML logic touched -- this bump is version-lockstep housekeeping only. Prior: v648 (2026-08-21, Task 0, HTML-only regression fix): the new "Admin Tools & Diagnostics" tab (v647) rendered ZERO cards live -- a legacy function _v196TidySettings() (predates the v647 reorg) unconditionally forced v177-tools-index-card's dataset.settingsTab back to 'panel' on every Settings render and every tab click, silently overriding _V73_SETTINGS_TAB_MAP's correct 'admintools' assignment (v133-admin-tools-card is dead code since v383, a literal false ternary, never in the DOM -- moot either way). Fix: that function no longer force-resets v177/v133 to 'panel' (kept for _v191BioCard, unchanged, since the map already sends it there too); the classList/DOM-reposition/display-toggle steps it also does are preserved for all 3 ids, since v177's markup is authored above the tab bar in the template and needs that reposition to render in the right place. Broadened audit swept every dataset.settingsTab reference in the file -- confirmed no other function independently overrides the map. Also expected to fix the Panel/Master tab's stale "(15)" live count -- 14 real panel-mapped cards plus v177 wrongly force-counted as panel; should read "(14)" post-fix. No GAS logic touched. No cached-asset-list change beyond the routine CACHE key bump. Prior: v647 (Phase 6 Step 3d, Task B): HTML-text-only fix -- new "Admin Tools & Diagnostics" Settings tab; the v177 tools-index-card + the already-disabled v133 legacy card moved out of the Panel/Master grab-bag into their own small tab (same pattern as v644's Compliance & Legal). No GAS logic touched. No service-worker-relevant change. Prior: v646 (Phase 16 follow-up): HTML-text-only fix -- Settings "Full backup mirror" card's table count was stale after v645's own promotion (still said 27/39, corrected to 25/41; the "19 graduated out" list/count also updated to include Shift_Freeze_Snapshots + FnF_Waivers, now 21) -- caught by v645's own second independent audit pass rather than left stale, matching the exact bug class every prior table promotion in this card's history (v530/v548/v567/v615/v618/v620/v638/v639/v640) already fixed as part of the same change. No GAS logic touched. No cached-asset-list change beyond the routine CACHE key bump. Prior: v645 (Phase 16, Task C): GAS-only fix -- Shift_Freeze_Snapshots + FnF_Waivers promoted from the once-daily backup-blob tier to the ~5-min periodic mirror (row-index-synthesized keys, FullFinal explicitly excluded, owner-approved). No HTML/client logic touched. No service-worker-relevant change. Prior: v644 (Phase 6 Step 3d, Task B): NEW "Compliance & Legal" Settings tab -- T&C moved out of Panel/Master into it; POSH Register (previously entirely unmapped, falling into Panel/Master purely by the tab-map's own default fallback) added and moved into it too. Pure UI regroup -- no card's function/behavior changed, only which tab it's found under. Prior: v643 (Task A discovery): GAS-only fix -- perEmployeeClockLockEnabled (the v624 experimental clock-lock flag) was never added to handleUpdateSettings' VALID_CONFIG_KEYS allow-list, so every save attempt since v624 has silently dropped it (ok:true, key ignored) -- found live while attempting the owner-approved Phase 13 concurrent-clock-in test. Prior: v642 (Task 0b): added a videoWidth/videoHeight>0 guard to _essCaptureDescriptor before calling faceapi.detectSingleFace, matching the same dimension-check-before-touching-canvas pattern already used at 2 of this file's 3 getImageData sites (_v332ShotQuality/_v340AntiSpoofCheck; the 3rd, a fixed-320x240-literal profile-photo capture, is immune a different way and was never guarded) -- the strongest identified (not fully proven) candidate for Sentry ARTZFOLIO-HRMS-8's IndexSizeError on ESS mobile face-login. Prior: v641 (Task 0a): GAS-only fix -- root-caused + fixed the confirmed Tickets Postgres dual-write failure (sbAutoSyncTick's per-table skip-if-unchanged signature cache never re-tried a frozen table even after its Supabase copy was separately lost; now cleared whenever the daily reconciliation check independently proves a mismatch). Prior: v640: HTML-text-only fix -- replaced v639's "most/a few" approximation (itself found inaccurate by 2 independent audit passes -- only 19/39=49% genuinely graduated from this tier, not "most") with an exact, source-verified 19/8/11/1 three-way split re-derived directly from every table's own GAS case comment; also dropped an overstated "6 PII-sensitive" label the code's own comments contradicted for 2 of the 6, and moved the version-history trail into a hidden HTML comment matching this card's own established v530/v548/v567/v615/v618/v620 precedent instead of leaving it owner-visible. No functional change, no count change (still 27/39). No service-worker-relevant change. Prior: v639: HTML-text-only fix -- reworded the same Settings card's "39 tables have now been promoted... from the Full backup mirror" sentence, which an independent second audit pass on v638 found falsely implied ALL 39 tables graduated from this specific backup tier. Prior: v638: HTML-text-only fix -- Settings "Full backup mirror" card's stale table count/version-range corrected (37->39 tables, v532-v620->v532-v634); real _V281_BACKUP_TABLES count also corrected (23->27, stale since v635 added 6 tables). Prior: v637 (Phase 6 Step 3c): Settings Cloud & Speed consolidation -- payroll-engine controls (golden-master + engine cut-over) now live ONLY in the Supabase Speed card, Fast Mode + auto-sync controls now live ONLY in the Migration & Speed Control Center card, migrate-grid trimmed 8->5. Prior: v636 (P27): removed FnF Records' client-direct stale (up to ~24h) quick preview -- see loadFnFRecords. Prior: v635 (P26): 6 zero-coverage money-critical tables (Salary_Payment_Status/Time_Penalties/FullFinal/SalarySnapshots/Shift_Freeze_Snapshots/FnF_Waivers) added to the daily Postgres backup mirror -- pure additive, no read/write path changed. Prior: v634: Phase 11 -- Activities promoted to a real
   structured table + instant-mirror (fixed the same 3-part gap class caught for Milestones at v633);
   client-side _v556ActivitiesFast updated to read flat columns instead of blob JSON. No cached-asset-list
   change -- CACHE key bump is version-lockstep housekeeping only. */
/* v633: Phase 12 -- Milestones promoted to a real
   structured table + instant-mirror; client-side _v558MilestonesFast updated to read flat columns instead
   of blob JSON. No cached-asset-list change -- CACHE key bump is version-lockstep housekeeping only. */
/* v632: Phase 10 -- GAS-ONLY change (closed 2 of 3
   flagged write-path instant-mirror gaps: OfferAcceptances, Bonus_Ledger). No HTML/frontend logic changed
   this round -- CACHE key bump is version-lockstep housekeeping only. */
/* v631: Phase 9 item 3 -- KRA Assessments (the "real
   bottleneck" per v607's own comment) now has a fast-read path (_v631KRAAssessmentsFast), same pattern as
   the existing KRA_Templates/FnF_Records/Performance_Reviews wrappers. Also fixed a real live bug in
   _v607KRATemplatesFast: it never passed the signed owner JWT hrms-read's v36 OWNER_GATED requirement
   needs, so it has 403'd and silently fallen through to the slow path on every call since 2026-08-16 --
   fixed. No new cached asset, no service-worker-relevant change -- CACHE key bump is version-lockstep
   housekeeping only. */
/* v630: Phase 9 item 1 -- Attendance's single-employee
   date-range view now has a fast-read path (_v303EmpAttRangeFast), reusing the month view's already-
   proven Supabase query shape with a range filter instead of a month-prefix filter. No new cached asset,
   no service-worker-relevant change -- CACHE key bump is version-lockstep housekeeping only. */
/* v629: Phase 6 Step 3a -- the "Atomic Go-Live Wipe" tool
   removed entirely (owner-approved), not disabled/hidden. Client modal + GAS handler + routing entry all
   deleted; grepped for stray references, none found. No service-worker-relevant change -- CACHE key bump
   is version-lockstep housekeeping only. See decision_log/2026.08.20_HRMS_v629_remove-golive-wipe_v1.md. */
/* v628: bugfix -- an independent pass-2 audit of v626 found
   Timesheets reads emp.billableRate (a compensation-adjacent field) on data that could come from v626's
   Employees-picker fast path, and the safe view backing that path carries neither salary nor billableRate.
   Reverted ONLY the Timesheets call site to the original slow-but-complete path; Attendance and Roster
   (verified to never read billableRate) correctly stay on the fast path. No service-worker-relevant change
   -- CACHE key bump is version-lockstep housekeeping only. See decision_log/
   2026.08.20_HRMS_v628_timesheets-billablerate-fix_v1.md. */
/* v627: Phase 5 (half-done table sweep) -- Activities
   promoted from the daily-only backup mirror to instant-mirror (its existing fast-read path was silently
   serving up to 24h-stale data). GAS-only this round, no HTML/client changes -- CACHE key bump is
   version-lockstep housekeeping only. See decision_log/2026.08.20_HRMS_v627_activities-instant-mirror_v1.md. */
/* v626: Phase 3 (Speed & Dependency Audit) -- a
   salary-safe fast-read for 3 employee-picker call sites (Attendance/Roster/Timesheets, verified to
   never display salary), the Reimbursements screen wired into its own pre-existing fast-read wrapper,
   and a diagnosable console/Sentry signal when any fast-read genuinely falls back to slow GAS. No
   service-worker-relevant change -- CACHE key bump is version-lockstep housekeeping only. See
   decision_log/2026.08.20_HRMS_v626_employee-picker-fastread_v1.md. */
/* v625: Phase 2 (REVISED) -- the standing time-based
   late-grace feature, the owner's actual ask. New "Standing Late Grace (Date Range)" panel in Attendance
   -> Late Penalty Waivers, alongside (not replacing) v623's Bulk Waiver. No service-worker-relevant
   change -- CACHE key bump is version-lockstep housekeeping only. See decision_log/
   2026.08.20_HRMS_v625_standing-late-grace_v1.md. */
/* v624: Phase 1 -- owner-delegated clock-in lock fix. New
   hybrid per-employee CacheService lock (handleClockIn/Out/BreakIn/Out), gated behind Config key
   perEmployeeClockLockEnabled, DEFAULT OFF -- flag-off behavior is byte-identical to v623. New Settings
   checkbox to flip it (experimental, do not enable without a live-tested rollout). No service-worker-
   relevant change -- CACHE key bump is version-lockstep housekeeping only. See decision_log/
   2026.08.20_HRMS_v624_clockin-peremployee-lock_v1.md. */
/* v623: Phase 2 -- new Bulk Late-Penalty Waiver tool (date
   range or custom date list, one or many employees, one action). Pure loop over the unchanged
   _v417ExcuseLateOne per-employee-per-day waiver -- no new money/deduction logic. No service-worker-relevant
   change -- CACHE key bump is version-lockstep housekeeping only. See decision_log/
   2026.08.20_HRMS_v623_bulk-late-waiver_v1.md. */
/* v622: Phase 1 clock-in lock-contention overlay/double-toast
   fix -- background confirmatory write no longer raises the shared "please wait" overlay, 'System busy,
   please try again' added to the soft-error suppression list, two stale comments corrected re:
   PendingAttendance's already-live fast-read path. No service-worker-relevant change -- CACHE key bump is
   version-lockstep housekeeping only. See decision_log/2026.08.20_HRMS_v622_clockin-lockcontention-phase1_v1.md. */
/* v621: mobile/kiosk stuck-overlay reliability fix --
   refreshEmpCard() (fires after every clock-in/out/break-start/break-end) and doCancelLeave()'s
   post-cancel refresh were raw, unretried API.call('auth',...) calls, the same protection-gap bug class
   already fixed for showEarnings() at v565. Wrapped both in the existing _v565RetryGas wrapper. Also
   removed the pointless refreshEmpCard() call inside the offline-punch-queue path -- it only fires after
   a genuine network failure is already confirmed, so retrying there only prolonged the stuck overlay on
   the worst-connectivity path. Root-caused via an owner mobile screenshot. See decision_log/
   2026.08.19_HRMS_mobile-stuck-overlay-rootcause_v1.md and decision_log/
   2026.08.19_HRMS_v621_mobile-stuck-overlay-fix_v1.md. */
   /* v620: P13 area 4 of 4 (FINAL) -- owner-approved
   full-scope Notice_Period_Records + ExitForm_Clauses promoted to real structured Postgres tables +
   instant write-mirror, same pattern as the prior 3 P13 areas. HTML-side changes: the Settings
   backup-mirror card's static table-count text corrected (25->23 mirrored, 35->37 promoted), and the
   ExitForm_Clauses client-side fast-reader (_v555ExitFormClausesFast) updated to read the new
   real-column shape instead of the old {_rk,rowjson} blob. See decision_log/
   2026.08.19_HRMS_v620_noticeperiod-exitform-realtable_v1.md. */
   /* v619: HTML marker bump only -- this round's real work is
   GAS-side + Supabase-side (owner-approved FK between OfferLetters and Candidates; fixed a pre-existing
   bug where the public job-application webhook's source field was silently dropped on write). See
   decision_log/2026.08.19_HRMS_v619_offerletters-fk-and-candidatesource-fix_v1.md. */
   /* v618: P13 areas 2-3 of 4 -- Candidates + OfferLetters
   promoted to real structured Postgres tables + instant write-mirror, same pattern as Pulse Survey (v615).
   Both confirmed 0 rows before promotion. HTML-side change: the Settings backup-mirror card's static
   table-count text corrected (27->25 mirrored, 33->35 promoted). See decision_log/
   2026.08.19_HRMS_v618_candidates-offerletters-realtable_v1.md. */
   /* v617: HTML marker bump only -- this round's real work is
   GAS-side (Tier C empty catch(){} remediation, 63 catches across 30 functions/areas, same documented
   safe log-and-continue pattern as v615/v616 -- zero control-flow/return-value change, provably
   correctness-preserving by inspection per CLAUDE.md rule 14(b)). See decision_log/
   2026.08.19_HRMS_v617_tierC-emptycatch_v1.md. */
   /* v616: HTML marker bump only -- this round's real work is
   GAS-side (Tier D empty catch(){} remediation, 61 of the audit doc's explicitly-detailed Tier D rows
   across ~55 functions, same documented safe log-and-continue pattern as v615's Tier C+D round -- zero
   control-flow/return-value change, provably correctness-preserving by inspection per CLAUDE.md rule
   14(b), no live verification needed). See decision_log/2026.08.19_HRMS_v616_tierD-emptycatch_v1.md. */
   /* v615: HTML marker bump only -- this round's real work is
   GAS-side, see the GAS file's own v615 comment for the complete change list: Pulse Survey promoted from
   the daily blob mirror to a real structured Postgres table + instant write-mirror (P13, first of 4 named
   areas), and 65 Tier C+D empty catch(){} blocks fixed with the documented safe log-and-continue pattern
   (Logger.log added, zero control-flow/return-value change -- provably correctness-preserving by
   inspection, per CLAUDE.md rule 14(b), no live verification needed). One HTML-side change: the Settings
   backup-mirror card's static table counts corrected (29->27 promoted tables; also caught and fixed a
   pre-existing, unrelated "36" staleness in the same card). See decision_log/
   2026.08.19_HRMS_v615_pulse-realtable-and-emptycatch_v1.md. */ /* v614: closes the one remaining pass-2 audit finding from
   v611 -- LoadingOverlay's forceReset() (fires on every screen navigation) could let a slow screen's
   late-arriving hide() decrement a brand-new screen's own show()/hide() count, hiding the overlay over
   still-loading content for a few seconds. Fixed with a discarded-hide-call counter that absorbs exactly
   the number of hide() calls the reset generation still owes -- zero call-site changes anywhere in the
   file, no service-worker-relevant change. See decision_log/2026.08.19_HRMS_v614_overlay-race-fix_v1.md.
   */ /* v613: 3 more items from the pending-work list -- no
   service-worker-relevant change, CACHE key bump is version-lockstep housekeeping only. (1) Mobile-
   screenshot incident (error toast + a success shown together): the clock-in/out fast-ack success paths
   (personal + kiosk + kiosk-optimistic-queue) now tag their background write so a lost-answer on it shows
   an accurate "couldn't confirm" warning instead of the old "NOTHING was changed" text that contradicted
   the success toast already on screen. (2) The "resigned employee still selectable" pattern v611 fixed in
   4 bulk-attendance pickers is now fixed in 21 more money/leave-adjacent pickers app-wide (shift/salary/
   timing change, admin leave, transactions, F&F, comp-off, attendance tools, roster, bonus management,
   payment status, emergency clock actions), all via the same canonical _v611ActiveVisibleEmps() filter,
   with single-employee panels keeping a pre-selected employee visible even if hidden so a panel can never
   silently swap to someone else. An independent audit of this sweep then found a real gap in 4 of those 21
   sites (Advance-on-behalf, Admin Add-Leave, Add-Transaction, F&F) plus 3 more Settings pickers reached via
   a separate shared helper (Salary Revision Simulator, Attendance Regularisation, Employee Documents):
   their searchable-datalist widget's suggestion list was correctly filtered, but the widget's own value-
   RESOLVER (resolveEmpSearch) searched the full unfiltered employee list independently, so typing a hidden
   employee's exact name/ID could still select them — fixed by having the widget stash the exact filtered
   list its suggestions were built from and resolving against that instead. (3) Structural (not just error-handling) speed fixes for Roster, Settings,
   Timesheets, Payroll and the ESS Dashboard's kiosk-card chain -- serial, mutually-independent backend
   calls converted to Promise.all batches (wall-clock only, zero read-order change); Roster and Payroll also
   gained the same nav-stamp repaint guard Timesheets already had, closing a latent stale-repaint gap. Zero
   salary/leave-balance/attendance-calculation logic touched by any of this. See decision_log/
   2026.08.19_HRMS_v613_pickersweep-perfstructure-toastfix_v1.md. */ /* v612: 2 owner-approved perf tweaks, no service-worker-
   relevant change -- CACHE key bump is version-lockstep housekeeping only. (1) sbAutoSyncTick (GAS side)
   now throttles its heavy background-sync pass to at most once per ~10 min during the 08:00-09:30 IST
   clock-in rush, instead of every 5-min tick, unless it's the belt-and-braces hourly full pass. (2)
   _v303TwoPhase's Dashboard 'today' call site now skips its redundant background GAS call when the last
   authoritative result is under 5 min old (Timesheets/per-employee-grid call sites untouched, per the
   performance analysis' own risk assessment). See decision_log/2026.08.19_HRMS_v612_perf-peakhour-tweaks_v1.md.
   */ /* v611: multi-screen reliability fixes -- stuck "please wait" overlay now clears on every screen navigation + becomes click-to-dismiss after 15s; Approvals "Final Approve"/Leave Ledger approve/admin auto-approve no longer swallow a lost/404 or thrown-network-error response silently (loud toast + safe read-only reload of the real status, never a blind write retry); Auto-UL bulk-delete (both entry points) now retries a lost/404 answer safely (idempotent delete-by-id); Employees "0 shown" now distinguishes a genuine empty result from a failed fetch (retry-wrapped read + explicit "couldn't load / Retry" state instead of a silent empty table); Bulk Hours Entry / Range Backfill / Bulk Attendance Excel (grid + template) employee pickers now exclude hidden/resigned employees, matching the Employees page's own canonical filter; face-registration profile-photo save failures are no longer silently swallowed. No service-worker-relevant change -- CACHE key bump is version-lockstep housekeeping only, same as v602/v580 before it. See decision_log/2026.08.19_HRMS_v611_multiscreen-reliability-fixes_v1.md. */ /* v581: SalaryLadder promoted after a proper schema reconciliation (legacy unkeyed table renamed to backup, fresh table created with real level|shiftHours key, OWNER_GATED, backfills from the live Sheet on first sync tick -- no client fast-read this round). See decision_log/2026.08.14_HRMS_v581_salaryladder-migration_v1.md. v580_prior_note: HTML-ONLY, marker bump. Fixed stale "34 tables" Full-backup-mirror Settings card text (Batches 2-4) -- found live against production v575 (which itself still shows the even-older "36 tables", since v577's fix never deployed) while doing a live P0 check. True count is now 29 (after this session's v579 Batch 6 promotions removed Leave_Balance/Emp_Change_History/Emp_Bonus_Eligibility from the array, on top of the pre-existing Bonus_Ledger/CompensationConfig staleness fix). Also corrected the promoted-table enumeration, which had not been updated since v567 and was missing 9 tables promoted since (POSH_ICC/OneOnOneNotes/OnboardingChecklist/TnC_Acknowledgements/PettyCash/OT_Claims/Leave_Balance/Emp_Change_History/Emp_Bonus_Eligibility, plus Bonus_Ledger/CompensationConfig). No GAS logic change. See decision_log/2026.08.14_HRMS_v580_backupcount-textfix_v1.md. v579_prior_note: Leave_Balance + Emp_Change_History + Emp_Bonus_Eligibility fast-reads (Batch 6 final sweep) -- see decision_log/2026.08.14_HRMS_v579_leavebal-changehist-bonuselig-structured_v1.md. v578_prior_note: LoyaltyWallet Monthly Ledger + LoyaltyBonusLedger History owner-only fast-read wrappers added (speed pass). LoyaltyWallet's withdrawal-eligibility calc (getLoyaltyWalletStatus) deliberately NOT fast-read -- genuine 3-model money-critical computed logic, same reasoning as PettyCash. See decision_log/2026.08.14_HRMS_v578_loyalty-ledger-fastreads_v1.md. */ /* v577: PettyCash + OT_Claims promoted to real structured Supabase tables + OT_Claims owner-only fast-read wrapper added to renderOTClaims (see the GAS-side HRMS_VERSION comment for full detail; decision_log/2026.08.14_HRMS_v577_pettycash-otclaims-structured-mirror_v1.md). */ /* v554: fixed this swCode fallback blob -- it fed the owner-visible version/cache-key footer with a stale v548 stamp (this string), and contained THREE back-to-back un-terminated 'const CACHE' re-declarations below which would have thrown a SyntaxError if this fallback path ever actually executed (real sw.js failing to load). Collapsed to this single valid statement; all prior version history below is now pure comment text, not re-declared code. */ /* v548: Roster promoted to a real structured table (synthesized composite key weekStart|empId, since Roster has no natural single-column PK) with a client fast-read replicating handleGetRoster's leave-overlay/self-heal/synthesize logic, verified via a 9-case parity test against a GAS-faithful reference plus a 12,000-case IST date-arithmetic check -- inert until the hrms-read Edge Function's ALLOWED list separately activates it (held pending a live post-deploy sync verification). Also added a Tickets owner+manager fast-read (0 live rows, no money). Roster panel now shows a loading indicator instead of a silent multi-second wait. */ /* v536: GAS-only, zero HTML logic change -- ESS login now mints a Supabase JWT too (Phase 2.1b step 1 of 5), same mechanism already proven for admin logins. No client code reads or sends this JWT yet -- deliberately inert this release, see GAS changelog. */ /* v535: Leave_Conflicts promoted to a real structured Supabase table + RLS (5th table, after Leaves/Attendance/CompOff/Warnings); 3 live rows migrated with 0 mismatches; backup-array count 44->43; status card + tooltip text updated to match */ /* v534: fixed a real, live, pre-existing text bug -- the 'Sync ALL backup tables now' button title said 'Copies all 40 backup tables...' (stale since v526-v529 grew the array past 40; missed by v530's earlier '40 tables' fix, which only touched the Full-backup-mirror card description + the Verify-ALL button title, not this Sync-ALL button). Corrected to 44 (current _V281_BACKUP_TABLES length as of v533). No function, button behavior, or logic changed -- pure text fix, found and flagged during the v533 audit, fixed here as its own tiny surgical release. */ /* v533: Warnings promoted to a real structured Supabase table + RLS (4th table, after Leaves/Attendance/CompOff); 8 live rows migrated with 0 mismatches; backup-array count 45->44; status card + tooltip text updated to match */ /* v531: golden-master PASS status card + removed one-time RLS test button (was v525, never clicked, checks proven via direct DB verification) — see changelog */; // v530_stale-40tables-count-fix 2026-08-08. LIVE-AUDIT-FOUND BUG, HTML-only, two static description strings under the "Full backup mirror" card said "40 tables" -- true count is 46 as of v529 (Tickets/Candidates/OfferLetters/Assets/PulseQuestions/PulseResponses added v526-v529), a genuine stale-count defect this text has no way to self-correct since the client has no live read of the server-side _V281_BACKUP_TABLES array. Both occurrences corrected to 46. No function, button, or logic changed -- pure text. // v529_pulsesurvey-backup-mirror 2026-08-08. GAS + Supabase schema only, zero HTML logic change -- 'PulseQuestions'+'PulseResponses' added to the nightly backup mirror, no new UI. Closes the last of the 4 zero-backup-coverage modules. // v528_assets-backup-mirror 2026-08-08. GAS + Supabase schema only, zero HTML logic change -- 'Assets' added to the nightly backup mirror, no new UI. // v527_recruitment-backup-mirror 2026-08-08. GAS + Supabase schema only, zero HTML logic change -- 'Candidates'+'OfferLetters' added to the nightly backup mirror, no new UI. // v526_tickets-backup-mirror 2026-08-08. GAS + Supabase schema only, zero HTML logic change (marker-only) -- 'Tickets' (Helpdesk) added to the nightly backup mirror, reuses the existing Sync/Verify buttons, no new UI. // v525_rls-pilot-selftest 2026-08-08. Adds one owner-only Settings button, "Run Full RLS Test (Leaves pilot)", calling new GAS action rlsPilotSelfTest -- automates the 4 remaining checks (self-scope / cross-employee denial / expired JWT / bad signature) on the v_pilot_leaves_self_select RLS policy applied live 2026-08-08. No other logic touched. // v524_config-header-fix-retroactive-plus-jwt-test-mint 2026-08-07. TWO items, both GAS-side, this HTML file's edits are the two version markers only. (1) RETROACTIVE DOCUMENTATION of a live incident fixed by a DATA edit, not a code edit: Config sheet cell A1 (the header row every getCfg()/sheetToObjects() lookup depends on) was corrupted from the literal text "key" to "kA1ey" -- root cause unknown (not caused by any release in this changelog), effect was server-side apiToken lookups returning empty, which is what produced "This device is not authorized" on every device including ones that had never needed to re-enter a token before. Fixed by editing the one corrupted cell back to "key" (owner-approved before the edit), verified live via the app's own pingToken diagnostic (before: serverLen 0; after cache TTL expired: serverLen 41, match true) and by a full reload rendering correct Config-dependent data. No HTML, GAS or SW code was broken or changed to cause or fix this -- the code was always reading the cell correctly, the cell's own content was wrong. (2) NEW: an owner-only diagnostic button, "Test Supabase JWT Mint", that calls the new GAS action mintTestSupabaseJwt to mint a short-lived test JWT via the existing v520 infrastructure (_v520MintSupabaseJwt_, live-armed now that SUPABASE_JWT_SECRET is configured) and verify it against the real Supabase database by attempting a read on the PunchDefers table (which carries the v518 deny-all-to-anon/authenticated RLS policy) using ONLY the JWT for auth, never the service_role key. A 401/403 means the signature is invalid (wrong secret or malformed token); an HTTP 200 (regardless of row count, since RLS denies all rows to this role) means the signature validated correctly. The JWT itself never leaves the server -- the button reports only a pass/fail verdict, never the raw token. This proves the v520 minting infrastructure actually produces a Supabase-valid JWT before any RLS policy is ever written to depend on it, closing the loop opened in v520 ("cannot be live-tested... until the owner adds that secret") now that the secret exists. // v521_backupsync-timebox-resume 2026-08-07. GAS-only, zero HTML logic change (version markers only). Fixed backupSyncTick (the 40-table nightly Supabase backup mirror) hitting GAS's ~6-minute execution ceiling and getting killed mid-run 2 nights running (8/5, 8/6 -- seen live via GAS failure-notification emails). Was an unbounded loop with no time budget and no resume point; now time-boxed to 5 minutes with a resumable cursor in Script Properties, same pattern already proven on the sibling function sbAutoSyncTick (v490). The "last successful backup" timestamp Settings reads only advances once a full 40-table pass actually completes across however many runs it takes -- a partial night can never look finished. // v520_supabase-jwt-mint-infra 2026-08-06. Phase 2.1b infrastructure ONLY, GAS-side, zero HTML logic change (this file's edits are the two version markers only). Added _v520MintSupabaseJwt_ -- a real RFC 7519 HS256 JWT minted at admin/owner/manager/supervisor login (handleAdminLogin, both success branches), shaped for Supabase's GoTrue/PostgREST so a FUTURE RLS policy can read custom claims (empId, adminRole) instead of every read going through hrms-read's service_role key. KILL-SWITCHED: reads Config key SUPABASE_JWT_SECRET; empty/missing (confirmed empty 2026-08-06 via two independent checks) means the function returns null and the login response is byte-identical to v519 -- no client change needed or made. No RLS policy anywhere depends on this yet. Cannot be live-tested against real Supabase until the owner adds that secret (Project Settings -> API -> JWT Keys -- a different, more sensitive credential than the anon/service_role keys already in Config; no available MCP tool exposes it). ESS and kiosk logins are deliberately untouched this release -- kiosk has no signed-in identity, ESS is a separate, larger blast radius for its own release. // v519_boot-settings-retry-fix 2026-08-06. Root-caused live: v516's Employees Supabase fast-path silently broke the _v260Boot retry's ability to detect a Settings/GAS-specific cold-start failure, because "any of the 3 boot calls succeeded" stopped meaning "GAS is healthy" once Employees could succeed independently via Supabase. Fixed by also retrying when Settings specifically didn't come back ok, not only when all three failed. Activities deliberately excluded (confirmed genuinely 0 rows, no benefit to retrying it). HTML-only, one added condition, no other logic touched. // v518_settings-fastpath-plus-rls-hardening 2026-08-06. Added a Supabase fast-path read for getSettings, wired ONLY into the two cold-boot call sites (_v260Boot, loadAppData) -- ~14 other call sites deliberately left on the untouched GAS path (several are post-save verification reads where a Supabase mirror lag would be actively dangerous). Guards against ConfigPublic being empty before the first save since deploy (treated as not-ready, falls back to GAS, never fabricates an empty settings object). Separately, database-side only, no HTML/GAS logic change: Phase 2.1 RLS hardening -- 53 explicit deny-all policies for anon/authenticated added to every table that had RLS enabled with zero policies (verified live before and after that this changes nothing for the app, since all real reads go through Edge Functions on service_role and bypass RLS entirely), and the rls_auto_enable() SECURITY DEFINER advisory closed properly (REVOKE EXECUTE FROM PUBLIC; verified the trigger itself still auto-enables RLS on new tables). Security advisor: 2 WARN + 53 INFO -> zero findings. v515_pendingatt-reimb-mirror-plus-backupsync-repair 2026-08-06. HTML unchanged (this release is GAS-side: mirror list + trigger repair) -- version bumped for 4-way lockstep only. GAS side: added 'PendingAttendance' and 'Reimbursements' to the _v490Json mirror rotation (owner request) using the SAME _sbMirrorJson_ path already proven for 9 other tables -- data now flows into Supabase, no client fast-read wired yet (that's a separate, later step, same discipline as every prior migration stage: mirror -> verify parity -> THEN read from it). SEPARATELY, found LIVE during this session that the 40-table backup-sync trigger (_V281_BACKUP_TABLES, daily 2am) had gone stale -- last real sync 2026-08-03, three days behind despite the trigger existing in code since v281. Re-installed the trigger live via the already-built, owner-gated installBackupSync handler, then ran an immediate full catch-up sync. No code bug caused the staleness -- the trigger had simply been removed/reset at some point (this app deletes-then-recreates triggers by name during several repair operations, e.g. v477's fix, which can drop an unrelated trigger if run at the wrong moment). v514_employees-fastpath-deadtable-fix 2026-08-06 changelog preserved below for provenance: REAL FUNCTIONAL FIX, found in the same live-audit session as v513: renderEmployees()'s Supabase fast-path, and the Settings "Speed Test" diagnostic, both hardcoded the table name 'employees_public' -- a view REMOVED long ago (before v511/512) after it was found to leak salary/mobile/dob. Since that removal, EVERY attempt to use this fast path has silently 403'd and fallen back to slow GAS -- meaning "Fast Mode ON" for Employees has done nothing, ever, since that security fix shipped, and the Speed Test button has always incorrectly reported the fast path as "failed." Live-reproduced: clicking Speed Test returned "Direct Supabase: 1902ms (failed)". Root cause found by grepping the whole file for the dead table name after finding one broken caller and asking whether there were others -- there were, exactly one more. Fixed both call sites to use 'employees_roster_v', the real, live-proven-safe replacement (0 banned columns). Checked all 42 downstream consumers of the resulting array for any salary-field access before making this change -- zero found, safe. v513_syncgrid-tooltips-liveaudit 2026-08-06 changelog preserved below for provenance: HTML-ONLY, ZERO LOGIC CHANGE. Owner deployed v512, then asked for a live audit of everything built. Drove the LIVE v512 tab via Chrome MCP: expanded all 6 redesigned Settings cards for real, found one genuine gap the static-HTML edit pass in v512 could not have caught — the 8-table Sync/Check grid in the Migration Control Center card (id v280-tables) is built at RUNTIME by v280RenderTables(), so it had zero title tooltips even after v512, including on SalaryRecords and SalaryRegisterMeta, the two most sensitive buttons on the page. Fixed: every Sync/Check button in that grid now has a title tooltip; SalaryRecords/SalaryRegisterMeta/Transactions get an explicit "money-adjacent" badge + a note that syncing them only refreshes the backup mirror and never exposes salary to the browser (hrms-read's allow-list still excludes both — verified live same day). Everything else in v512 was live-audited and found correct: 4-way version lockstep confirmed live, all 6 cards render with their EVERYDAY/SECURITY/DANGER ZONE badges intact, toggleCard()+localStorage persistence works, golden-master button proven live (see decision log). v512_admin-console-descriptions 2026-08-05 changelog preserved below for provenance: HTML-ONLY, ZERO LOGIC CHANGE — every onclick/function untouched. Rewrote the Settings tab's 6 Supabase/Speed/Health cards (~50 controls total) from a wall of unlabeled buttons into a plain-language admin console: every button now carries a persistent visible one-line description (not just a hover tooltip, since factory-floor use is often touch-only) plus a risk badge — EVERYDAY (safe any time, never touches pay), SECURITY/VERIFICATION (read carefully, built to fail safe), or DANGER ZONE (salary-critical, gated behind golden-master PASS). Card 1 (Speed & Health, the one on screen when this was asked for) got the full treatment: 11 buttons regrouped into EVERYDAY (monitor/diagnose) and SECURITY (the 4 cloud-key controls). Cards 2-4 (Direct Read, Keep-Warm, Attendance Parity) and the two Advanced cards (Migration Control Center, Supabase Speed Sync & Health) got matching badges and filled in every previously-untooled button with a title tooltip. Not a single onclick handler, function name, id, or backend call changed — verified by diff against v511 and by re-reading every edited region end to end. // v511_supabase-boot-reads-stage1 2026-08-05. STAGE 1 OF THE SUPABASE MIGRATION (Option A, non-salary reads only). SCOPE, STATED HONESTLY: the plan named five boot reads to move -- getPendingAttendance, getPendingLeaves, getTodayAttendance, getReimbursements, keepWarmStatus. Only TWO were actually movable today, and the other three are not moved as an oversight -- they are architecturally blocked, and here is why. getTodayAttendance already had a fast path (_v303TodayFast via _v303TwoPhase, live since v488) at the Dashboard call site -- untouched here; its OTHER call site (the kiosk Who's-In panel) was DELIBERATELY reverted off Fast Mode in v492 because the fast read carries no employee names, so it is left exactly as v492 set it. getPendingLeaves reads the Leaves table, which IS mirrored to Supabase, and already had a fast path at ONE call site (the Approvals Inbox, v315 -- untouched). This release extends the SAME pattern, behaviour-identical on any miss, to the three call sites that still went to Apps Script on every single call: refreshApprovalsBadge() (fires at every admin login), renderDashboard() and renderLeaves() -- none of the three pass an approverRole filter and none need more than the leave list/count, so the plain Leaves-table read is a safe substitute, exactly as at the existing v315 site. NOT MOVED, verified live against the Supabase database before writing a line of code: getPendingAttendance reads the PendingAttendance sheet and getReimbursements reads the Reimbursements sheet (which carries claim amounts -- money-adjacent). NEITHER sheet is in the Supabase mirror -- _sbResolveTable_ lists only Employees/Leaves/Attendance/Transactions/Points/Holidays/SalaryRecords/SalaryRegisterMeta, and a direct query of the live database confirms no PendingAttendance or Reimbursements table exists there at all. Building a fast path for either would first require adding the sheet to the GAS mirror rotation, backfilling it, verifying the count matches, and widening the hrms-read Edge Function's table allow-list -- a materially bigger and separate change, deliberately left for its own release rather than rushed into this one. keepWarmStatus was also not moved: it answers 'is a specific Apps Script trigger installed', which is Apps Script's own live state with no Supabase equivalent to read, and it never runs on the boot path at all -- only from a Settings status card. THE NEW HELPER: _v511PendingLeavesFast() reuses the existing _sbDirectRead/_v265Flag/_v424NonSalaryFastAllowed gate unchanged (Fast Mode owner-only by default, broadened to manager/supervisor for the Leaves table specifically, exactly as v424 already allows), reads the Leaves table once, and refuses to trust the answer if it comes back at or above the 1,000-row PostgREST page cap (283 rows live today -- the guard cannot fire yet, but is in place before the table grows into it). Any doubt at all returns null and the caller falls straight through to the untouched API.call. ONE flag reverts every change here: az3_directRead=off (the existing Fast-Mode toggle) makes all three new call sites behave byte-for-byte as they did in v510. NOTHING about salary, attendance, leave, timesheets, payslip or payroll logic is touched anywhere in this release; calcMonthlySalary is untouched. Timed live before/after on the panel -- see decision log 2026.08.05_HRMS_v511-supabase-boot-reads-stage1. // v502_exit-type-and-attendance-safety-card 2026-08-05. FIX-1, AND IT WAS A SILENT DATA FAULT THE OWNER FOUND BY ASKING A QUESTION: 'how do I mark somebody terminated, or absconding?' The answer was that you could not. The Direct Exit button sent exitType 'Mutual' HARD-CODED, so every immediate exit - a dismissal for cause, an absconding, a contract simply ending - was filed as a mutual separation on the Exits and Notices record, in the F&F, and in every export made from them. The free-text reason might have been right; the CLASSIFICATION was always wrong, for everybody, and nothing on screen revealed it. Direct Exit now ASKS - Resigned, Terminated, Absconding, Mutual, Contract Ended, Retired - defaults to nothing so it cannot be answered by accident, refuses to run until it is answered, states the choice in the confirmation, and the backend stamps it on the EMPLOYEE record as well as the notice row, so a report can finally tell a termination from a resignation. The Employees sheet already had an exitType column; this path had simply never written to it. The two other exit routes are unchanged and were already correct: 'Initiate Notice / Exit' for a resignation or termination served with notice, and Approvals - Exit Alerts - 'Archive to FnF draft' for an absconding the system has detected from consecutive unapproved-leave days, which already records Absconding. FIX-2, FINISHING v501'S SAFETY NET. The auto-UL circuit breaker - the check that stopped the system marking 26 of 29 staff absent on a day the app was down - lived entirely in the Config sheet: two thresholds nobody could see and a blocked-day alert that could only be cleared by hand-editing a cell. By this project's own rule that is not a finished feature. There is now a card, 'Attendance Safety - stop a bad day marking everyone absent', under Settings - Shifts & Time, showing whether a day is currently blocked and why, with the two thresholds editable and a button to clear the alert once the day has been set by hand. NOTHING about salary, attendance, leave or payslip logic is changed: calcMonthlySalary is md5-identical for the fourteenth consecutive release - BUT SAID PRECISELY, because an audit caught the sentence being broader than the truth: choosing ABSCONDING does change the F&F settlement. That is existing, deliberate policy in handleCalcFnF - absconding forfeits leave encashment and adds the uninformed-absence penalty of roughly three days pay - and it simply never fired on this route before, because the route always said 'Mutual'. The confirmation now spells that out before it is agreed to, and the F&F PREVIEW button sends the same classification as the save, so the figure you check is the figure you get. AUDIT FIXES INSIDE THIS RELEASE, two of them blockers, both the same shape - a card that reports success while doing nothing: getSettings returns an ARRAY of key/value rows and the card read it as an object, so it always showed the built-in defaults and printed a green 'no day has been blocked' even on a day one HAD been blocked; and the three new config keys were never added to the backend's allowed-keys list, so handleUpdateSettings silently rejected every write and still answered ok:true - Save and Clear were decorative. Both fixed, and both buttons now check what actually happened, distinguish a timeout from a failure, and refuse to show anything at all if the reply is not the shape it should be. Also fixed: one vocabulary for exit types, matching the values Initiate Notice already stores, so a report cannot end up treating Resigned and Resignation as two things. // v501_slip-bundle-and-auto-ul-breaker 2026-08-04. FIX-1, WHY 'ALL SLIPS' TOOK EIGHT MINUTES - and it was never the PDF. jsPDF, html2canvas and JSZip all run in this browser and are fast. Building each slip fired THREE separate Apps Script calls, once per employee: the day-by-day projection, the leave balances and the reimbursements. For 29 staff that is about 87 round trips at 7-24 seconds each against a backend that allows four at a time - precisely the eight to fifteen minutes measured live on 4 August, behind a 'Please wait' that said nothing. All three are now fetched for EVERYONE in one chunked call (new backend action getSlipBundleBulk), and each slip is seeded with the answer so the three existing per-employee enrichers short-circuit on their own idempotency guards. No slip layout changes, no figure moves, and if the prefetch fails for any reason the old per-employee path runs exactly as before - slower, never wrong. Applied to all three slip buttons: the combined PDF, the separate PDFs and the ZIP. FIX-2, A REAL PROGRESS BAR. The slip pack now shows 'Preparing salary slips 12 of 29' with a bar and a plain explanation, including while it waits out a busy backend, and the bar is removed on every path including failure. A silent multi-minute wait is indistinguishable from a hang - that is the same lesson this project learned on the payroll screen, applied here. NOTHING about salary, attendance, leave or payslip layout is changed in this file: the only edits are the prefetch helper, the row-seeding helper, the progress helper, three slip call sites and one entry in the silent-actions list. // v500_retry-rounds-and-keep-cancellations 2026-08-04. EVERYTHING IN THIS RELEASE CAME OUT OF RUNNING v499 AGAINST THE LIVE SYSTEM RATHER THAN READING IT. FIX-1, AND IT IS THE ONE THAT MATTERS: v499 decided that a VOIDED or REJECTED prize row does not count as awarded, so a recompute would create it again. An audit argued for that and the reasoning was plausible. The live sheet says it is wrong. Every row in the production Bonus_Ledger is a Perfect Attendance Bonus for 06/2026 carrying 'VOIDED FULL 2026-07-10 by owner: Poor performance' - the owner deliberately withheld Rs 4,000 from four people, and Perfect Attendance has never excluded voided rows from its own duplicate check, so those four have correctly stayed unpaid. v499 would have made the points prize behave the opposite way: cancel a prize, recompute, and the money comes back. A recompute must NEVER resurrect a payment the owner has cancelled. Voiding is a decision, not a draft. A voided or rejected row now counts as awarded, exactly as Perfect Attendance has always treated it; to genuinely re-award a month, DELETE its prize rows in the Bonus_Ledger sheet - there is no delete-bonus button in the app, only void - or add the corrected prize by hand through Bonus and Loyalty. Both are deliberate acts, which is the point. The concern that motivated v499 - a half-written round frozen forever - is already solved by writing the whole round in ONE atomic call. FIX-2, THE ROUNDS NOW FINISH. Timed against the live backend: v499's 75-second rounds returned nine employees when Google was quiet, four when it was not, and the third round was abandoned by the browser at 150 seconds. No employee is slow - the SAME person failed and then succeeded two minutes apart. Apps Script was queueing, because this account also runs twelve background triggers. At rest between bursts a plain call answers in 5.9 to 8.7 seconds. So: rounds drop to 55 seconds (measured: five employees, 66 seconds of wall clock); there is an eight-second breather between rounds so Google can drain instead of refusing; each round is RETRIED up to three times with a growing pause before the register is declared incomplete; and a payroll round is allowed its own 300-second ceiling instead of the 150 seconds an ordinary button gets. The retry is what actually made the run complete - a round failed twice with a 404 and succeeded on the third attempt, where v499 would have thrown the whole register away. Retrying is safe precisely because of Option B: re-running the same cursor recomputes the same people and a prize already awarded for the month is never written twice. HONEST ESTIMATE, CORRECTED: 15 to 25 minutes for a full month, not the 10 to 15 v499 claimed. PROVEN LIVE THIS ROUND: ten employees accumulated across rounds with ZERO duplicates and strict A-Z order; the cursor, the roster fingerprint and the accumulation all behave; Abhishek Joel computed to 16714.67, matching his July payslip to the paisa. NO salary formula, attendance rule, leave rule, payslip layout, payout value or default is changed: calcMonthlySalary is md5-identical for the release in this chain and in fact unchanged since v464. The one thing that DOES change is a pay-run SETTING, said plainly rather than hidden behind the usual sentence: the default size of a recompute round drops from 75 to 55 seconds and its ceiling from 80 to 70. AUDIT FIXES INSIDE THIS RELEASE, because the first draft of it was withheld too: the retry loop originally retried ANY unsuccessful answer, which would have re-sent a finalised month's polite refusal four times behind the words Google is busy - it now retries ONLY a lost or timed-out reply, never a considered no; a wall-clock deadline of 45 minutes was added, because a round cap alone allowed a theoretical eleven-hour run behind a promise of twenty-five minutes; the abort message now quotes the ceiling that actually applied rather than the general one; and three comments that still described the v499 behaviour this release reverses were deleted. THE 15 TO 25 MINUTES IS AN UPPER BOUND, not a measurement: five employees in 66 seconds implies about eight minutes for 29 people if nothing is dropped, and the wider figure is there to cover the retries that the live run showed are normal. // v499_one-button-payroll-and-option-b 2026-08-04. THE OWNER DECIDED OPTION B: a prize is awarded ONCE, at first approval, and never again. That decision is implemented in the backend, and it is what finally makes a working Recompute All possible. THE ONE BUTTON. Google kills any Apps Script request at SIX MINUTES; a recompute costs about fifteen seconds per employee, so 29 staff need about seven and a half minutes in one request. Recompute All was never slow - IT DID NOT FIT, on every version ever shipped. This screen no longer sends one request: it asks the server to compute as much as fits, take a marker, and carry on from that marker, round after round, until the server says done - showing a progress bar and a plain explanation while it works, because a silent two-minute wait is indistinguishable from a hang. Same engine, same people, same figures; the request is divided, the calculation is not. THREE FAULTS OF THE WITHHELD v497, CLOSED BY CONSTRUCTION: (1) a partial run is never presented as complete - what arrived is labelled INCOMPLETE with the number of people missing and the reason; (2) that warning is rendered INSIDE the register's own banner slot, so the container repaint that erased it in v497 now renders it; (3) Save to Records REFUSES an incomplete register outright, because saveSalaries overwrites the whole month and a part-finished run would leave uncomputed people carrying stale figures. THREE BUTTONS BECOME TWO, and the duplicate is gone: 'Recompute All', 'Recompute Selected' and 'Recompute (live)' were three buttons running TWO commands - 'Recompute (live)' called the identical function as 'Recompute Selected'. There is now one button that does the whole month and one that does only the people you tick, both plainly labelled, and the third has become a whole-month recompute where it sits on the saved register. NOTHING about salary, attendance, leave, timesheets or payslip layout is changed in this file: the only edits are the recompute call, the incomplete banner, the save guard and the button labels. // v498_release-the-stuck-slots 2026-08-04. ONE BUG, AND IT IS WHY THE KIOSK AND THE DASHBOARD STOPPED. This app allows only FOUR backend calls at a time. A call that finishes releases its slot; but the fetch had NO TIMEOUT AT ALL, so a call Google never answers NEVER settles and NEVER releases its slot. Four unanswered calls consume every slot permanently, and from that moment EVERY other call queues behind them forever - the dashboard never finishes loading, the kiosk sits on 'processing', and nothing recovers until the page is closed. That is exactly the reported behaviour: 15-20 minutes for a dashboard and then nothing, and staff unable to clock in. It also explains why a raw call typed into a console answered in 6.9 seconds while the app itself was frozen - the console does not go through the limiter. FIX-1: the WHOLE wait is now budgeted, not just the fetch. The clock starts BEFORE a call queues for a slot, so a call that spent its budget waiting gives up at once instead of starting a fresh wait of its own. Reading the reply body now also happens inside the budget and inside the slot - a reply that stalls half-way through would otherwise hang forever one layer down - and the honest cost of that is that a slot is held a few milliseconds longer than before. The slot is released on EVERY path, including a browser too old to cancel a request at all. AN UNCLAIMED BONUS FOUND BY THE AUDIT: in v496 a call that never settled kept the in-flight counter above zero forever, which PERMANENTLY DISARMED the v493 stuck-curtain watchdog. v496 therefore had the deadlock and a defeated watchdog at the same time. Bounding the call re-arms it. FIX-2, and an audit correctly called this a blocker: a lost answer returns early and was therefore skipping the one toast this app relies on to announce a failure. Twenty-five screens read an answer without checking it first, so a lost answer was landing as a green '0 employees clocked out', a Rs 0 balance, or 'No salary records found'. Every lost answer, every timeout and every command that could not be sent now says so in plain words, once, before it returns. A failure must look like a failure. A SECOND AUDIT CORRECTION INSIDE FIX-2: the first draft reused the eight-action retry list written in v492 for the slow owner buttons, but only ONE of those eight is ever reached on this path - so 27 of the 28 calls a login fires would have had no retry and the app would not have healed without a reload. Any plain read now retries; deliberately NOT anything that merely is not a write, because calcAllSalaries would pass that test and it appends prize rows. AND THE TWO FAILURES ARE NOW TOLD APART, which matters on the factory floor: a request that TIMED OUT may have run, so a punch must not be blindly re-scanned; a request that gave up while queueing NEVER REACHED GOOGLE AT ALL, and the screen now says press it again. FIX-3: the payroll screen catches a thrown call and shows the error instead of spinning forever. FIX-4, stated precisely because a false safety claim has preceded three of the last five incidents: the cache wrapper was swallowing an error thrown by the call itself and then quietly sending the same command again, so a write could be attempted SIX times. It is now at most THREE, and a write that fails that way announces itself instead of clearing its spinner in silence. Repetition on a plain network error is REDUCED, not eliminated. ONE CEILING, ONE PLACE: the slow owner buttons waited 150 seconds while ordinary calls waited forever; both now read a single constant, set to 150s. It is deliberately NOT lower - a 5-employee payroll recompute was measured at 119.7s, so a shorter ceiling would have started killing work that legitimately runs today. WHAT THIS DOES NOT DO, said plainly: it does not make Apps Script fast. If the backend is merely SLOW rather than stuck, a 150s ceiling now turns some very long calls into visible failures that v496 would have waited out - that is the intended trade, because a screen that says so and retries beats a screen that hangs. And Recompute All for all 29 staff still cannot finish inside Google's 6-minute execution limit; that needs the resumable server-side run, which is the next release. NOT IN HERE: the batched Recompute All built as v497 was AUDITED AND WITHHELD - five faults in its own new code, including that batching changes the money because the bonus ledger is snapshotted per request. It is parked in _WITHHELD_v497_do_not_deploy and must not be deployed. No salary formula, attendance rule, leave rule, payslip layout, payout value or default is changed anywhere in this release: calcMonthlySalary is md5-identical and the backend has ZERO logic changes. // v496_month-closes-only-when-everyone-is-paid 2026-08-04. TWO FAULTS, both root-caused from the owner's own screenshot showing a green LIVE FIGURES banner above 'Rs 0 Total Net Payroll, 0 Employees'. FAULT-1, THE EMPTY REGISTER. Apps Script now takes 54-60 seconds on a whole-sheet read and past that point Google ABANDONS the request and answers with its own status page - a body that looks like {status:ok, app:ArtzFolio HRMS v495, version, ts}. That is VALID JSON, so the parse guard added in v492 let it straight through, and the payroll screen's guard tested res.ok === false, which is FALSE for a reply that has no ok field at all. The screen then fell through to res.salaries || [] and rendered an EMPTY register under a green LIVE banner. A failed fetch was presented as a successful payroll run of zero employees - the exact rule this system is built on breaking. FIX: API._rawCall now RECOGNISES the status-page shape and turns it into a real failure (and releases the loading curtain, which the v493 lesson requires), and the payroll screen now demands res.ok === true before it will render a single figure. Anything else is an error message, never an empty register. FAULT-2, THE MONTH THAT CLOSED ITSELF. The owner is right and the old rule was wrong. Since v172 the freeze read: if ANY ONE approved salary row exists for a past month, the WHOLE month is FINALIZED. One employee left, his F&F was generated and saved, and that single row closed July for all 29 people. Worse, an F&F-settled employee is DELIBERATELY EXCLUDED from the monthly register by the v61 double-count guard - so the month was being closed by a row belonging to somebody who is not part of that register at all. NEW RULE, which is the owner's own words: a month is finalised ONLY when every employee who belongs in that register already has an approved salary record. The expected population is the full Active / Notice / Notice Period list MINUS anyone settled through F&F for that month, and it is measured before any employee-picker narrowing so recomputing three people cannot make a month look closed. If even one person is still unpaid the month stays OPEN and recompute proceeds normally, as many times as needed. The freeze itself is kept for the genuine case - a fully paid month is still protected and still overridable. NO salary formula, no attendance rule, no payout value and no default is changed: calcMonthlySalary remains md5-identical. v495_close-the-salary-leak-for-real 2026-08-03. Carries v493 (frozen-screen fix + watchdog) and v494 (boot-weight removal). THE HEADLINE IS A LIVE P0 THAT I HAD PREVIOUSLY REPORTED AS CLOSED AND HAD NOT. On 2 August the salary leak was 'fixed' by removing the view employees_public from the hrms-read allow-list. That allow-list is a rule inside one Edge Function. It is NOT what PostgREST consults. The view itself still existed, was SECURITY DEFINER, and the anon role still held SELECT on it - and the anon key is printed in the app's own public HTML. So for the last day and a half, anybody on the internet could read every salary and every mobile number with a key that ships in the page. REPRODUCED, not theorised: a plain GET to rest/v1/employees_public with that key returned id, name, salary 14000, mobile 9573943885. CLOSED, and proven closed: employees_public is DROPPED; employees_roster_v and attendance_month_v are now security_invoker=on with SELECT revoked from anon and authenticated; anon now gets permission denied on both and 'table not found' on the dropped one, while the app's own path through hrms-read (service key) still returns 53, 1666, 244 and 1699 rows. THE LESSON, recorded so it cannot happen a third time: an allow-list inside one function is not an access control - it only governs the door it sits on. Postgres grants and RLS govern the database. A leak is not closed until the ORIGINAL attack is re-run and fails. Also this round: the sanitised roster view employees_roster_v was created and PROVEN clean (explicit column list, zero of 34 banned money and PII columns, checked in the schema AND in the live payload) and is allow-listed in hrms-read v8 - but the client code to consume it was REMOVED before shipping, because an independent audit proved it was dead code with no caller on any screen, and because designation is filled in for only 9 of 53 employees and department for 10, so it would have delivered nothing visible today. The view is in place and locked down for when those fields are filled in. This HTML is therefore byte-identical to v494 apart from the two version markers. NOTHING about salary, attendance, leave, timesheets, payslips or payroll logic is touched anywhere in this release. // v494_take-the-weight-off-the-boot 2026-08-03. THIS RELEASE CARRIES v493's OUTAGE FIX AND THEN TAKES WORK AWAY, because the honest finding is that the speed project has been ADDING load, not removing it. MEASURED LIVE TODAY against the real backend: an action with NO HANDLER AT ALL - the dispatcher simply reports unknown action - took 14.6 SECONDS. getSettings took 14.6s. keepWarmStatus took 14.9s cold and 5.6s warm. That is the floor: every single Apps Script call costs about 6 to 15 seconds no matter what it does, so the ONLY thing that makes a screen faster is MAKING FEWER CALLS. And I had been adding them. v473 added a background getReadToken refresh on every boot; v489 added a background getPunchToken refresh on every boot; v489/v491 then made each of those calls ALSO do a Supabase network round trip AND take an Apps Script script lock, on the boot path, competing with clock-ins. AUDIT CORRECTION, because overstating a fix is how the last three went wrong: the honest saving is ONE Apps Script call on most devices and TWO on the owner's, not two everywhere, and ZERO on a first-ever boot where both keys still have to be fetched. Those calls were background work, not on anybody's visible clock. The real win is narrower and still worth having: it frees one or two of the four concurrency slots during the boot storm, and it removes one or two acquisitions of the app-wide script lock that clock-in queues behind. v494 removes exactly that and nothing else: both refreshes are now time-boxed to once every 12 hours, and the repair path is not weakened but sharpened - a key that is actually REFUSED (401 from the cloud) sets a repair flag that forces an immediate re-fetch on the next call and asks the backend to re-push it. A working key is never re-fetched; a broken one is repaired at once. Also carried from v493: the reference-counted Please-wait curtain that froze the app is fixed at its source and given a watchdog floor. NOTHING about salary, attendance, leave, timesheets, payslips or payroll is touched anywhere in this release - the only files changed are the two token refresh paths, one 401 branch, and the version markers. // v493_release-the-stuck-please-wait 2026-08-03. ONE MISSING LINE TOOK THE APP DOWN, AND IT WAS MINE. After v492 the app sat on Please wait forever, across refreshes, for two hours. PROVEN LIVE, not guessed: the backend was healthy the whole time (getSettings answered with valid JSON in 14.6 seconds, serverVersion v492), the page had rendered completely behind the curtain, and there was not one console error. The overlay is REFERENCE COUNTED - show() increments a counter, hide() decrements it, and it only lifts at zero - and the JSON-parse guard I added to API._rawCall in v492 RETURNED from inside its catch without calling hide(). One unparseable reply therefore left the counter stuck above zero and the curtain down permanently, with the working app and the working backend sitting behind it. Forcing the overlay off in the owner's live browser revealed a fully functional app, which is what identified it. FIX-1: that path now hides the overlay before it returns. FIX-2, and this is the one that matters more: a counter that can only be corrected by perfectly balanced code will fail again the next time anybody writes an early return, so it now has a floor under it. A watchdog checks every ten seconds and, if the curtain has been up for more than 90 seconds with no real activity for the last 20, releases it, records the reason, reports it to crash reporting and tells the owner in plain words. A stuck screen is now a 90-second inconvenience instead of an outage. No salary formula, no attendance rule, no payout value and no default is changed anywhere in this release. // v492_undo-the-cachebuster-and-simplify-payroll 2026-08-03. FIX-1 AND IT IS MY OWN REGRESSION: every slow owner button broke after v490 - Check the Cloud Keys returned a SyntaxError about an unexpected < , and Rotate the Kiosk Punch Key, Check and Repair Triggers and the payroll comparison all timed out at 150 seconds. ROOT CAUSE PROVEN by an A/B test from the owner's own browser against the live backend, same action and same body, four calls: WITHOUT the cb parameter 200 and valid JSON in 14.9s then 5.6s; WITH it 200 in 20.0s once and then HTTP 404 with an HTML page in 38.0s. Apps Script does not tolerate an unknown query string on a POST to exec - it answers with a 404 web page, the browser cannot parse that as JSON, and that is exactly the error on screen. The cache-buster I added in v490 to stop a rare lost reply instead caused a total failure of every slow button, and it is removed. Kept from v490: the explicit content type, never auto-retrying a command that is unsafe to repeat, and recognising a page-instead-of-an-answer, now reported in plain English with the HTTP code instead of a raw parser error. FIX-2 THE KIOSK PANEL SHOWED EMPLOYEE CODES INSTEAD OF PEOPLE, also mine: v491 read that panel through the kiosk punch key, which deliberately carries NO names, and joined names from a local cache that is empty on a phone. A screen that lists people must show people, so the panel is back on the Apps Script read that carries the full identity, getTodayAttendance now also returns designation, and every row shows the complete identity - name, designation and department, shift window, and since-time - the same golden name structure used everywhere else. The punch-key fast read is kept where it needs no names: the kiosk clock-in/clock-out direction check. FIX-3 PAYROLL IS SIMPLE AGAIN. Recomputing a finalised month had become a research project with two jargon buttons in the wrong order. The plain truth was buried: RECOMPUTE ONLY WORKS OUT AND SHOWS THE FIGURES, it does not write the saved register, and nothing is saved until Save to Records is pressed. So the panel now leads with one button that just does it after one plain confirmation, and the two comparisons are demoted to clearly numbered optional steps in the order they should be used, each labelled with how long it takes. The comparison also honours the employee picker so it can answer in seconds for a few people instead of timing out on the whole company. No salary formula, no attendance rule, no payout value and no default is changed anywhere in this release. // v491_settings-snapshot-and-kiosk-fastread 2026-08-03. CORRECTION FIRST: v490 said a status-page reply meant 'the command never ran and nothing was changed'. Live evidence disproves it - the cloud read key has been rotated TWICE and the originally leaked key is DEAD (proven: hrms-read now refuses it). The rotations ran; only their answers were lost. The true statement is that a status-page reply means THE REPLY IS LOST AND YOU CANNOT TELL WHETHER THE COMMAND RAN, which is exactly why a command that is unsafe to repeat is never retried automatically. v491 fixes that wording everywhere and adds the missing piece: a Check the Cloud Keys button that reads the live state - which keys exist by their last six characters only, whether a retiring key is still inside its grace window, and the last rotations from the Audit log. FEATURE-A, the permanent answer to 'recomputing uses current settings': every approved register now captures the ENTIRE non-secret Config sheet plus CompensationConfig into a new Payroll_Settings_Snapshot sheet, so from now on what a month's settings were is answered by reading, not guessing. Everything is captured rather than a curated pay-relevant list, because a hand-written list of keys that matter is the exact mistake that left a credential in an open endpoint. A new button on the FINALIZED panel lists every setting that has changed since the register was approved; for a month approved before snapshots existed it says so honestly instead of inventing an answer. FEATURE-B, the kiosk stops asking Apps Script who is in: hrms-punch v6 gains a read-only today op authorised by the kiosk's own punch key, returning today only and only empId, clock-in, clock-out, breaks and session - NO NAMES, no money - with punches from the last few minutes folded in so it is fresher than the 5-minute mirror. Measured live: 17 rows in 358ms against an Apps Script call that was failing outright during the 8.26am rush. Names are joined locally from a cache in this file, so no name ever travels on the punch key. No salary formula, no attendance rule, no payout value and no default is changed. // v490_four-live-faults 2026-08-03. Four faults hit on the live v489 build, all four root-caused against the running system. FAULT-1 the Rotate the Cloud Read Key button said 'Not changed: no reply from the backend' - PROVEN live from the owner's own browser that the POST was answered by Google's plain status page (body {status, app, version, ts}), so the command never ran and nothing was changed. Every slow-button POST now carries an explicit content type and a unique cache-buster, recognises that status-page reply, retries once by itself, and says exactly what happened if it recurs. FAULT-2 Google emailed that the mirror sync exceeded the 6-minute execution ceiling and was KILLED, leaving every table after it unsynced with no way to resume - the sync is now time-boxed to 4 minutes and resumable from a cursor, and the changed-since stamp only advances on a COMPLETE pass so a partial run can never look finished. FAULT-3 the kiosk told staff the attendance counts were UNKNOWN - that guard is correct and stays, but it now tries twice five seconds apart before saying anything, and repeats the warning at most once every ten minutes instead of every five. FAULT-4 Payroll refused to recompute a FINALIZED month and could only warn vaguely that current settings might differ. Most pay settings carry no history, so nobody can prove what July's settings were - guessing would be dishonest. So v490 MEASURES instead: a new read-only comparison runs the exact same computation the recompute would run, in memory, writes nothing anywhere, and reports employee by employee whether the saved register would change and by how many rupees. No salary formula, no attendance rule, no payout value and no default is changed anywhere in this release. // v489_kiosk-gets-its-own-key 2026-08-03. THE ROOT CAUSE OF THE 2 AUGUST LEAK IS NOW CLOSED, not just its surface. What was wrong: ONE key did two different jobs. The public config.json gave out the apiToken, the apiToken alone unlocked getReadToken, and the key that came back both READ the cloud copy AND was the key the punch function accepted - so a stranger could read Leaves and Attendance, and could also forge a clock-in. That key could not be locked down or changed either, because the kiosk on the factory floor is not signed in to anything and had nothing else to identify itself with: either change would have stopped every scan. THE FIX IS A SECOND KEY. The punch path now asks for its own credential (new action getPunchToken); the punch key records a punch and can read NOTHING, and the read key reads and cannot punch - both proven live on 2026-08-03 against the real Edge Functions. With the kiosk no longer needing the read key, the v487 login guard is turned ON: the cloud read key is now issued only to a device that is genuinely signed in. And because the read key is no longer holding the factory floor hostage, it can finally be CHANGED - two new Settings buttons in the Speed and Health card, Rotate the Cloud Read Key and Rotate the Kiosk Punch Key. A rotation pushes the new key to the cloud FIRST and only writes the sheet if the cloud accepted it, so a failed rotation changes nothing; the retired read key keeps working for a 15-minute grace window so no screen breaks mid-use; and every kiosk repairs itself after a punch-key rotation because its own 2-minute keep-warm ping is what notices the refusal. Every failure path ends in the untouched Apps Script route: slower for one scan, never wrong, never blocked. NOTHING about attendance, salary, leave or payroll logic is touched anywhere in this release, and the only setting changed anywhere is READTOKEN_REQUIRE_LOGIN off to on, which is the entire point of the release. // v488_payroll-page-shows-payroll 2026-08-02. THE REPORTED BUG, root-caused and fixed. Opening Payroll showed the DASHBOARD instead - sidebar said Payroll, title said Payroll, content was the dashboard - plus a stray warning toast, and it only came right after a third refresh. ROOT CAUSE: _v303TwoPhase is a fast-paint then authoritative-repaint helper. It paints instantly from the cloud copy, then fires a repaint callback when the slow Apps Script answer finally arrives. The dashboard's callback is renderDashboard(), which writes straight into admin-tab-content - and NOTHING checked whether the user was still on that screen. Apps Script takes 5-33 seconds, which is far longer than it takes to click Payroll, so the late answer landed on top of whatever screen had replaced it. Refreshing 'fixed' it only because it left no stale promise in flight. FIX-1: _v303TwoPhase now remembers which screen asked, and silently skips the repaint if the user has navigated away. One guarded change, all four call sites, and behaviour is byte-identical when the user has NOT moved. FIX-2: the kiosk Who-In-Out refresh ran on a global 5-minute timer and raised its warning toast on whatever screen happened to be open - it now does nothing unless its own panel is actually on screen. FIX-3: the Payroll empty state now names the month it is showing and says how to change it, because at the start of a month it defaults to the new month and reads as if the data had vanished. NO setting, salary rule, payout value or default is changed anywhere in this release. // v487_readtoken-login-and-narrow-reads 2026-08-02. (A) SECURITY, the root cause of today's leak. Proven live: the PUBLIC config.json gave the apiToken, that alone unlocked getReadToken, and that key returned every employee's salary, mobile, date of birth and home address. The exposed view was withdrawn immediately (hrms-read v5), but the door itself was still open and Leaves and Attendance were still reachable through it. getReadToken now requires a REAL SIGNED-IN SESSION - either a valid owner token or a valid HMAC role token, both of which every logged-in device already sends on every call (v91 P1). An anonymous caller holding only the public apiToken is refused. Failure is graceful by construction: _v265Init already falls back to Apps Script and _v474FastAck already returns null, so the worst case is slower, never broken. Kill switch Config READTOKEN_REQUIRE_LOGIN = off restores the old behaviour without a redeploy, and refusals are audit-logged once per 10 minutes so a legitimate caller cannot hide. (B) SPEED, the worst read in the app. _v327LoadTodayMap pulled the ENTIRE 1,671-row attendance table into the browser to build a map of TODAY - on the kiosk, on the punch path. It now asks for one day. _sbDirectRead gained optional date-range narrowing matching hrms-read v5 (dateCol/fromDate/toDate); every existing caller passes no range and is byte-identical. NO setting, no salary rule, no payout value, no default and no variable is changed anywhere in this release. // v486_where-pay-comes-from-and-scan-routes 2026-08-02. Two owner-visibility additions, both in the Speed & Health card so nothing has to be hunted for. (A) WHERE YOUR PAY IS CALCULATED FROM. The owner could not find the payroll-engine source setting -- it lives four cards further down in Advanced - Supabase Migration & Speed Control Center, which is a reasonable place to hide a switch and a terrible place to hide an ANSWER. The Speed & Health card now states it in one plain line every time the card is opened. It says Google Sheets, or Cloud copy in red, or -- if the check itself fails -- that it could not tell. It never guesses the safe answer. (B) WHICH ROAD DID RECENT SCANS TAKE. Until now a deferral left no trace anywhere: the Edge Function handed a reason to the browser and the reason died there, which is exactly why four versions were spent hunting a bug that did not exist. hrms-punch v4 now records every deferral fire-and-forget (not awaited, so it adds zero time to a scan), a new PunchDefers table holds them for 30 days, and a new button reports the last 24 hours grouped by reason in plain English. Also shown: the route the last scan on THIS device took. No attendance, salary, leave or payroll logic is touched. // v485_daily-health-report 2026-08-02. (A) DAILY HEALTH REPORT: a new Settings button, 'Send Me the Health Report Now', runs five live overnight checks and emails the verdict; the backend also sends it by itself at 07:00 every morning. The headline is built FROM the findings, and a check that could not be read is shown as a FAILURE, never as a pass. (B) PUNCH PATH VISIBILITY: the kiosk confirmation panel now states, in words, WHICH route the scan took -- Instant (about a quarter of a second) or the normal Google backend -- and when the instant route declined, WHY. Proven live on 2026-08-02: the reason the punch queue stayed empty through v474-v484 was never a bug; every test scan was past the 60-minute late-approval threshold and the Edge Function correctly sent it for approval instead. The system was right and simply never said so out loud. This makes it say so. No attendance, salary, leave or payroll logic is touched. // v484_instantpunch-reaches-the-kiosk 2026-08-02. THE BUG, found by auditing the live v482 app: Instant Punch was switched ON and working, yet PunchQueue was empty and Verify Every Punch reported Checked 0 scans. ROOT CAUSE: _v474FastAck was wired into only TWO places, the admin and ESS clockIn and clockOut functions. The KIOSK path that every QR, PIN and face scan on the factory floor goes through never called it at all. So the one screen that actually needed the quarter-second answer was still waiting the full several seconds on Apps Script, and the owner had been told the feature was on. It WAS on; it simply was not connected there. Every measurement, every safety net and every version note about Instant Punch since v474 was therefore true of the admin screen only. FIX: the kiosk punch path now asks for the same ~250ms verdict; on a clean yes it confirms to the employee, frees the kiosk, stops the scanner and releases the camera exactly as v479 requires, and lets the untouched Apps Script write run in the background with the same payload, same punchId, same handler, same Sheet and same salary engine. Any uncertainty defers to the original synchronous path unchanged. // v483_instantpunch-label-and-timesheet-autoretry 2026-08-02, two owner-reported faults, both diagnosed against the LIVE v482 app. FIX-1: the Instant Punch button always read Turn ON regardless of the real setting, so there was no way to tell its state without clicking it. Confirmed live that _v474FastOn was already true and az3_fastPunch was on -- the setting was correct all along, only the label lied. The label is now rendered from the live setting when the card is drawn, a permanent status line states ON or OFF in words, and a 3-second sync keeps it honest. FIX-2: the whole Timesheet week showed a question mark in every cell. The v473 guard was RIGHT to refuse to print a false absent mark, and the red banner was accurate -- but it was a dead end. Live re-verification proved the data was fine the entire time: BOTH paths returned 29 employees with real records for that exact week, the fetch had simply fired while the app was still booting. The screen now retries once by itself after four seconds, clearing its cached empty result first, and only shows the red banner if the retry also fails. Display-only; no attendance, leave or salary logic touched. // v482_failed-fetch-guard 2026-08-02. Makes the v473 Timesheet lesson reusable: a screen must never render a meaningful value -- an absent mark, a zero count, a rupee figure -- out of a fetch that failed or came back empty, because those are claims about reality and on a payroll system a wrong claim is worse than a visible error. New shared helper _v481Trustworthy. Applied to refreshWhoInOut, the kiosk Who-In-Out panel, which had no guard and rendered zero in / zero out when the call had simply failed. It now shows a question mark per count plus an explicit UNKNOWN warning. Display-only. NOTE TO FUTURE SELF: keep these changelog notes free of backtick characters -- a backtick pair in this comment broke the whole inline script in the first v481 attempt and node --check caught it. // v480_punch-reconciliation (2026-08-02). Instant Punch is ON by default from v479, so a scan is confirmed to the employee from Supabase in ~250ms while the authoritative Google Sheet write happens moments later in the background. That is safe by construction -- idempotent punchId, automatic replay, Sheets still the system of record -- but safe by construction is an ARGUMENT, not EVIDENCE, and this system pays people. v480 adds something that goes and CHECKS: a new read-only backend job compares every punch the fast path accepted against what is actually in the Attendance sheet and names anything missing, by employee and date. New Settings button 'Verify Every Punch Reached the Sheet' in the Speed & Health card, plus a nightly automatic run at 01:15 IST that writes a PUNCH_RECONCILE_ALERT to the Audit log if anything is missing -- so a silent loss cannot sit undiscovered until payroll day. Strictly read-only: never writes to Attendance, never touches pay, never modifies the queue. // v479_kiosk-stop-after-punch-and-instantpunch-default-on (2026-08-02). (A) KIOSK, owner-reported with video: 'the camera keeps showing the processing circle and stays open, it never says the scan is finished, and the next scan starts on top of it'. ROOT CAUSE: v432 FIX-B auto re-armed the scanner the instant a punch returned -- camera live again, QR detector running again, no completion state in between; v469's 8-second same-card debounce was treating the symptom. Applied the owner's rule instead: the scan screen CLOSES on a successful punch and the operator taps CLOCK IN / OUT again for the next person. The return path now hard-stops the QR loop, RELEASES the camera stream (stopping the loop alone left the video live, which is why the spinner stayed), and shows an unmissable bilingual confirmation panel with the employee name, IN/OUT and time, auto-clearing to the kiosk home after 6s. Nothing about the punch, the attendance write or pay is touched -- this is purely what happens on screen afterwards. (B) Instant Punch is now ON BY DEFAULT; only an explicit 'off' disables it. The automatic fallback is unchanged: any uncertainty in the Edge Function defers to the untouched Apps Script path. // v478_p0-triggerfix-and-safe-repair (2026-08-02, URGENT). v477 set the mirror-sync trigger to everyMinutes(3); Apps Script accepts ONLY 1/5/10/15/30, so the create threw -- and because the v474 self-healer deleted the old trigger BEFORE creating the replacement, sbAutoSyncTick was left with no trigger at all and the Supabase mirror sync stopped. Backend fix: interval 3->5, plus the self-healer now rejects an illegal interval before touching anything AND creates the replacement before deleting the old one, so a repair can never leave a job worse off. This file: the Check & Repair readout now shows any ERROR line in red with a red headline instead of burying it in a grey list (that is why the failure was easy to miss), and the Sentry organisation name in the test message follows the owner's rename to 'artzfolio'. // v477_syncgate-3min-and-readallowlist (2026-08-02, mostly a GAS + Supabase release; this file only adds the plain-language 'Cloud copy last refreshed N minutes ago' readout to the Check & Repair Triggers result). Backend: sbAutoSyncTick no longer reads all 17 mirrored tables in full every tick just to compute the MD5 that decides whether to skip -- one cheap Google Drive metadata call now answers 'has the spreadsheet been touched at all?', so an idle tick reads ZERO sheets. That is what allows the interval to go back DOWN from 15 to 3 minutes: faster, fresher AND lighter together. Safety: timestamp captured BEFORE the read so an edit during a sync is caught next tick; every 20th tick (~hourly) force-syncs regardless; any failure reading the timestamp behaves exactly as today. The v474 self-healer re-creates the trigger at the new 3-minute cadence on its own -- no manual reinstall. Supabase hrms-read v3 widened its allow-list by SIX company-wide reference tables only; every per-employee table stays out until Supabase Auth + RLS exists. // v476_slowbutton-patience-and-sentry-forceload (2026-08-02, two owner-reported v475 button faults, both root-caused live). FIX-01 'Test Error Reporting says crash reporting is not loaded': the Sentry LOADER script only installs a stub and defers the real SDK until the first crash, so window.sentryOnLoad never fired. Confirmed live: typeof Sentry==='object' with forceLoad/onLoad/init present but the SDK absent. Now Sentry.forceLoad() is called 10s after boot, and the test button uses Sentry.onLoad()+forceLoad() with a 12s honest timeout message. FIX-02 'Check & Repair Triggers -- could not check: no reply from the backend': the button used the shared API.call whose retry/timeout budget suits ordinary screens, but triggerHealth474 enumerates and can re-create GAS triggers and measured 5-30+ seconds live. The call was abandoned before Google answered and the error text wrongly implied a dead backend -- running the same action manually returned ok:true with ALL SIX background jobs healthy. Both slow owner buttons now use a dedicated _v476SlowCall with a 150s budget and a live on-screen seconds counter, and say 'busy, not broken' on timeout. No other call path touched. Carries v475/v474/v473. // v475_settings-buttons-and-crash-reporting (2026-08-02, owner feedback: "you keep telling me to run a function name or use the browser console -- where is the BUTTON?"). He was right; there was no UI at all for v474's new abilities. (1) NEW Settings card, exact label 'Speed & Health -- Instant Punch, Triggers, Queue', sitting directly ABOVE the existing 'Direct Read (skip GAS)' card, with four real buttons: 'Turn ON Instant Punch', 'Check & Repair Triggers' (calls triggerHealth474), 'Punch Queue Health' (calls punchQueueStats) and 'Test Error Reporting'. No console, no function names, plain-English result messages. (2) CRASH REPORTING via the Sentry loader script (public DSN key, safe in client code): async, fully try/catch-wrapped, nothing awaits it, so a blocked or slow CDN cannot affect the app. sendDefaultPii OFF; beforeSend redacts any key matching salary/ctc/pay/aadhaar/pan/bank/ifsc/token/password and drops the user object entirely; only the signed-in ROLE is tagged; replay and tracing both OFF. Flag az3_sentry='off' disables it. Carries v474 (instant punch + self-healing triggers) and v473 (3 fixes) unchanged. // v474_instant-punch-and-selfhealing-triggers (2026-08-02): (A) INSTANT PUNCH -- new Supabase Edge Function hrms-punch + PunchQueue table give a clock-in/out verdict in ~250ms (measured 310ms full round trip incl. all 4 index-backed queries) vs the Apps Script path which waits on an app-wide LockService lock shared by ~95 handlers AND reads the ENTIRE Attendance sheet while holding it. On a clean verdict the employee is confirmed and the kiosk freed IMMEDIATELY; the authoritative Apps Script write then runs in the background COMPLETELY UNCHANGED (same payload, same punchId, same handler, same Sheet, same salary engine), with the queued row as a replay safety net that cannot double-punch because punchId is idempotent (v327). Any uncertainty -- second punch same day, OT session, resume-after-clockout, late past the approval threshold, offline replay -- defers to the old path untouched. Flag az3_fastPunch, DEFAULT OFF: off = byte-identical to v473. (B) Carries v473's 3 fixes (Fast-Mode token cache-first, Timesheet loud-failure instead of a false 'A', bulk modals wait for the roster). Trigger self-healing is GAS-side, see the GAS changelog. // v473_fastmode-tokencache-timesheet-loudfail-rosterwait (2026-08-02, 3 owner-reported items, all root-caused against the LIVE app): (1) PERF -- _v265Init awaited a getReadToken GAS round-trip BEFORE reading the token already cached in localStorage, so Fast Mode was effectively OFF for the first ~2 min of every session (GAS measured 40-60s/call); now cache-first with a background refresh, stale-token path still falls back to GAS unchanged. (2) TIMESHEET all-blank/all-A -- renderTimesheets had no guard for an empty/errored getWeekAttendance result, so a slow or timed-out backend rendered a red A in every cell, indistinguishable from a real absence; live re-verification proved BOTH data paths return correct data (Supabase 1.06s, GAS 41.0s) so this was never data loss -- added a loud red banner + a gold ? cell instead of A. (3) BULK HOURS ENTRY 'No employees loaded' -- the bulk modals read the roster synchronously and bailed with a toast if it had not arrived; new _v473EnsureEmps() fetches on demand instead. Display/UX only: no salary, attendance, leave or payroll logic touched. // v472_autosync-interval-widen (2026-08-01, GAS-ONLY, this HTML file has zero logic changes this round -- version bumped purely for 4-way lockstep): live Google Apps Script Executions log showed sbAutoSyncTick (the Supabase Fast-Mode background mirror sync) occasionally taking up to 142s per run at its old 5-minute cadence -- root cause is the fixed cost of reading all 17 mirrored tables in full plus computing an MD5 signature on each, every single tick, regardless of whether the existing skip-if-unchanged optimization avoids the actual Supabase write. This is a standing background load competing with real user requests for the account's limited concurrent-execution slots, all day, not just during traffic bursts -- a likely major contributor to the general slowness reported. Widened to every 15 minutes in gas_backend_v472 (see GAS changelog); Fast Mode's 3 tables still refresh far more often than the pre-Fast-Mode daily-backup-only cadence. // v471_keepwarm-frequency-fix (2026-08-01)
const NETWORK_FIRST_HOSTS = ['cdn.jsdelivr.net','cdnjs.cloudflare.com','unpkg.com'];
self.addEventListener('install', e => { self.skipWaiting(); });
self.addEventListener('activate', e => {
  e.waitUntil(Promise.all([
    caches.keys().then(keys => Promise.all(keys.filter(k => k !== CACHE).map(k => caches.delete(k)))),
    self.clients.claim()
  ]));
});
self.addEventListener('message', e => { if (e.data && e.data.type === 'SKIP_WAITING') self.skipWaiting(); });
self.addEventListener('fetch', e => {
  const url = e.request.url;
  if (url.includes('script.google.com')) return;     // Never cache GAS API
  const isCdn = NETWORK_FIRST_HOSTS.some(h => url.includes(h));
  // v78: the app shell (this single-file HTML document + its inline JS = ALL of our code) is served
  // NETWORK-FIRST so a fresh deploy loads immediately and a fixed bug can never be masked by a stale
  // cached page (the recurring "the bug came back" trap). Offline still works via the cached fallback.
  // CDN libs stay network-first; other same-origin assets keep stale-while-revalidate.
  const isShell = e.request.mode === 'navigate' || e.request.destination === 'document' || url.indexOf('.html') !== -1;
  e.respondWith(caches.open(CACHE).then(async cache => {
    const cached = await cache.match(e.request);
    const fetchPromise = fetch(e.request).then(r => {
      // Only cache successful, opaque-safe responses
      if (r && r.ok && (r.type === 'basic' || r.type === 'cors')) {
        cache.put(e.request, r.clone()).catch(()=>{});
      }
      return r;
    }).catch(() => cached);
    return (isCdn || isShell) ? (await fetchPromise || cached) : (cached || fetchPromise);
  }));
});